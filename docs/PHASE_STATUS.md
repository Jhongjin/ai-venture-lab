# Phase Status

Use this file as the lightweight phase ledger for the agent work loop.

## Current Phase

- Phase: Automation-first product OS hardening
- Goal: Keep the idea-to-production-package flow aligned with the rule that AI prepares and the operator confirms only the important decisions.
- Status: `ship` for controlled beta; active hardening queue is focused on named external tool handoff, external production package clarity, task-led execution monitoring, and first-use simplicity.

## Loop Rules

1. Complete one coherent job.
2. Report what changed, what was skipped, validation results, commit, deployment state, and next job.
3. Commit and push repository changes.
4. Deploy user-facing changes and run production smoke.
5. Skip optional work when the current phase can advance without it.
6. Carry non-skippable external work as a named user action, then continue unblocked work.

## Previously Completed Core Scope

- Completed on: 2026-05-06
- Covered scope: idea extraction and validation gates, saved extraction portfolio reports, validation evidence coaching, PRD handoff, MVP slicing, Korean UX, app planning/design/development orchestration, development kickoff guardrails, implementation run packages, implementation task board, evidence quality gates, filtered task handoffs, artifact approval reviews, release decision packets, MVP build command packets, QA acceptance matrices, post-launch learning loops, versioned runbook artifacts, local release checks, Vercel production smoke.
- Remaining items are not blocking this phase because they require external access, optional AI tuning, or rerun-gated beta automation.

## Launch Gate Snapshot

| Field | Current value |
| --- | --- |
| Owner | Operator |
| Date | 2026-05-17 |
| Production target | `https://ai-venture-lab.vercel.app` |
| Technical smoke | Passed for `quality:full`, production shell/routes/browser, authenticated write smoke, RLS allowed/denied smoke, and telemetry smoke |
| Decision | `launch_gate_decision: ship` |
| Cleanup evidence | `cleanup_disposition_closed`; write-smoke and RLS fixtures are `retained_for_rerun`, telemetry/screenshot cleanup is `not_applicable` |
| Risk evidence | High beta-relevant risks are accepted, mitigated, or scoped out for controlled beta |
| Artifact approval evidence | PRD, MVP spec, design brief, and tech spec are approved for controlled beta scope |
| QA/security evidence | QA and security/privacy signoff are approved for controlled beta scope |
| Rollback evidence | Last-known-good deployment `dpl_72EhMSpuaz8r4PcjvPZ7uHipa8Sa`; owner Operator; rollback trigger/action recorded |
| Next decision | Controlled beta can proceed under the recorded constraints |

Validation keywords: `launch_gate_decision_ship`, `launch_gate_snapshot_recorded`, `cleanup_disposition_closed`, `risk_acceptance_recorded_for_controlled_beta`, `rollback_evidence_recorded_before_ship`, `last_known_good_deployment_recorded`.

## Completed Jobs

| Date | Job | Commit | Deploy | Validation |
| --- | --- | --- | --- | --- |
| 2026-05-29 | Extracted final execution package panel | `064eb7e` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:build-sync`; STEP 7's external-tool connection file/package card, start-prompt copy, primary setup download, and document-only download controls now live in `FinalExecutionPackagePanel`, with `FinalExecutionConnectionManager` passed in as the live-tool connection area. The large workbench Babel size warning remains for follow-up decomposition; no build pass was spent and the smoke account remained at 440 credits |
| 2026-05-29 | Extracted final execution connection manager | `01191cf` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:build-sync`; STEP 7's external-tool connection management, registry notice, connection health summary, connection list, and revoke controls now live in `FinalExecutionConnectionManager`, preserving `final-execution-connection-health` while reducing inline JSX in `idea-workbench.tsx`. The large workbench Babel size warning remains for follow-up decomposition; no build pass was spent and the smoke account remained at 440 credits |
| 2026-05-29 | Extracted final execution tool guide | `eb28100` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:build-sync`; STEP 7's external-tool command path, success criterion, detailed start guide, install/check command copy controls, MCP config copy, and generic package fallback now live in `FinalExecutionToolGuide`, preserving the final-execution smoke selectors while reducing inline JSX in `idea-workbench.tsx`. The first browser and build-sync smoke attempts hit local Windows `spawn EPERM`, then both passed on standalone rerun; no build pass was spent and the smoke account remained at 440 credits |
| 2026-05-29 | Extracted final execution readiness summary | `d7beff8` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:build-sync`; STEP 7's ready-state, product-surface, build-delivery, and final external-tool override summary now live in `FinalExecutionReadinessSummary`, preserving the same visible copy and tool-switch behavior while reducing inline JSX in `idea-workbench.tsx`. The large workbench Babel size warning remains for follow-up decomposition; no build pass was spent and the smoke account remained at 440 credits |
| 2026-05-29 | Extracted final execution quick start | `56abb08` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:build-sync`; STEP 7's `지금 할 일`, simple execution mode note, run-location summary, four-step quick path, setup checks, and tool-start mode now live in `FinalExecutionQuickStart`, preserving final-execution smoke selectors while reducing inline JSX in `idea-workbench.tsx`. The large workbench Babel size warning remains for follow-up decomposition; no build pass was spent and the smoke account remained at 440 credits |
| 2026-05-29 | Centralized no-store JSON helper | `42dcce3` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`, `pnpm smoke:build-sync`, `pnpm smoke:market`, `pnpm smoke:telemetry`; auth, AI, billing, build-sync, and telemetry response helpers now keep their domain-specific names while delegating no-store JSON response construction to `src/lib/no-store-json.ts`. Build-sync no-spend smoke passed with the smoke account still at 440 credits; market smoke returned `openai_web` with 5 sources and 4 competitors; telemetry smoke auto-created a disposable idea and passed without printing secrets |
| 2026-05-29 | Added no-store signup API responses | `9ab1efa` + smoke fix `db97766` | Pushed to `main`; production alias smoke passed after Git integration deploy | PowerShell parser check for `scripts/smoke_routes.ps1`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; `/api/auth/signup` now uses a no-store auth JSON helper for validation, server configuration, admin signup error, and success responses. Route smoke now verifies invalid signup returns 400 with no-store; a follow-up changed the smoke assertion to ASCII-only response-body validation after Windows PowerShell parsed the Korean literal incorrectly |
| 2026-05-29 | Added no-store AI route responses | `bd306bc` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_market_scan.mjs`, PowerShell parser check for `scripts/smoke_routes.ps1`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, direct production header check for `/api/ideas/extract`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:market`; `/api/ideas/extract`, `/api/ideas/generate-sample`, `/api/ideas/market-scan`, and AI route rate-limit responses now use no-store JSON responses. The first routes smoke saw the prior/partial extract header state and passed on retry after direct header confirmation. The first market smoke returned a transient HTTP 500, then the same synthetic request returned `openai_web` with no-store and the market smoke passed on retry with 5 sources and 5 competitors |
| 2026-05-29 | Auto-created telemetry smoke ideas | `3eb08cd` | Script-only; pushed to `main`; no app deployment required | `node --check scripts/create_telemetry_smoke_idea.mjs`, PowerShell parser check for `scripts/smoke_telemetry_ingest.ps1`, `git diff --check`, `pnpm smoke:telemetry`, `pnpm quality:full`; telemetry smoke now creates a disposable idea through the browser smoke account when the configured telemetry idea id is missing or stale, then retries the ingest once. The rerun passed with a newly created disposable idea and no secret values printed |
| 2026-05-29 | Added no-store telemetry ingest responses | `d907ce5` | Pushed to `main`; production alias smoke passed after Git integration deploy | PowerShell parser check for `scripts/smoke_routes.ps1` and `scripts/smoke_telemetry_ingest.ps1`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:telemetry`; `/api/telemetry/ingest` now shares a telemetry JSON response helper that sets `Cache-Control: no-store`, route smoke verifies the unauthenticated 401 cache boundary, and telemetry smoke verifies the successful ingest cache boundary. The first routes smoke saw the prior/partial header state and passed on retry after direct header confirmation. The configured local telemetry smoke idea ID was stale, so a new disposable telemetry smoke idea was created via the browser smoke account for this validation; no secret values were printed |
| 2026-05-29 | Clarified STEP 8 next-task fallback | `a8ae7bb` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:build-sync`; STEP 8's single-decision rule now tells users that if there is no completion evidence, they should keep only the next task and open detailed reports only when needed. No build pass was spent and the smoke account remained at 440 credits |
| 2026-05-29 | Showed STEP 5 completion criteria | `0582a4c` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_billing_credits.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; STEP 5's `제작 시작 패키지` now includes `완료 기준` alongside first message, first task, and connection file, so the package reads more like an executable development contract than a document bundle. No build pass was spent and the smoke account remained at 440 credits |
| 2026-05-29 | Clarified first-use current action | `4fa7644` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_browser.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; STEP 1 first-use intake now shows a `지금 할 일` card directly before the input box, saying to paste whatever comes to mind or leave it blank for AI-generated candidates, and the first-use guidance block now lives in `FirstUseIdeaIntake` with browser smoke coverage |
| 2026-05-29 | Extracted final execution setup checks | `eccc40a` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:build-sync`; STEP 7's folded installed-file list and external project-root check now live in `FinalExecutionSetupChecks`, preserving `final-execution-install-result`, `final-execution-root-check`, and folder-check smoke selectors while reducing inline final-execution JSX in `idea-workbench.tsx`. The large workbench Babel size warning remains for follow-up decomposition; no build pass was spent and the smoke account remained at 440 credits |
| 2026-05-29 | Extracted STEP 8 action summary | `23af365` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:build-sync`; STEP 8's immediate `지금 할 일`, one-line outcome, decision candidates, folded action ladder, and automatic sync brief now live in `Step8ActionSummary`, preserving the existing smoke selectors while reducing inline JSX in `idea-workbench.tsx`. The large workbench Babel size warning remains for follow-up decomposition; no build pass was spent and the smoke account remained at 440 credits |
| 2026-05-29 | Added no-store billing API responses | `96623af` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_billing_credits.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; `/api/billing/credits` and `/api/billing/build-pass` now share a billing JSON response helper that sets `Cache-Control: no-store`, and billing smoke verifies the cache boundary for anonymous credit, anonymous build-pass, and authenticated credit summary responses. No build pass was spent; the smoke account remained at 440 credits |
| 2026-05-29 | Centralized build-sync tool labels | `738ff3f` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:build-sync`; Cursor, Codex, Claude Code, and Google Antigravity labels/tool guards now share `src/lib/build-sync-tools.ts` across token issue, progress write-back, and registry errors. The first routes smoke attempt hit local Windows `spawn EPERM`, then passed on standalone retry; no build pass was spent and the smoke account remained at 440 credits |
| 2026-05-29 | Added no-store build-sync API responses | `cd5fffa` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, direct production header checks for `/api/build-sync/token` and `/api/build-sync/progress`, `pnpm smoke:build-sync`; build-sync token, progress, list, and revoke JSON responses now share no-store headers, and the no-spend build-sync smoke verifies the token/progress/list cache boundary. The first build-sync rerun saw the prior/partial header state, then passed on immediate retry; no build pass was spent and the smoke account remained at 440 credits |
| 2026-05-29 | Clarified market scan source boundary | `028e844` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:market`; STEP 3 market/competition scan results now surface a `근거 기준` sentence before the summary cards, separating source-backed research from estimate drafts before the result feeds production-package evidence. Market smoke returned `mode=openai_web`, `model=gpt-5-mini`, 5 sources, and 5 competitors |
| 2026-05-29 | Clarified first-use next-step unlock | `49411f1` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_browser.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; STEP 1 first-use fast path now says saving opens the bottom next-step button and continues into business evaluation, making the save-before-next rule visible before the input box |
| 2026-05-29 | Promoted STEP 8 judgment question | `2cfa143` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:build-sync`; STEP 8 now places `오늘 답할 질문` immediately under the `완료/다음/판단` summary so the operator sees the single decision before auxiliary order, options, sync detail, or reports. No build pass was spent; the smoke account remained at 440 credits |
| 2026-05-29 | Added STEP 7 final-execution success criterion | `5205888` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:build-sync`; STEP 7 now shows a `성공 기준` sentence that tells users the setup is complete when the confirmation command returns T-001, then directs them to paste the START file as the first tool message. No build pass was spent; the smoke account remained at 440 credits |
| 2026-05-29 | Extracted shared STEP 7/8 review grid | `d6b38d3` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:build-sync`; STEP 7 run-location and sync summaries plus STEP 8 simple/sync summaries now share `WorkbenchReviewGrid`, preserving smoke selectors while reducing repeated inline JSX in `idea-workbench.tsx`. No build pass was spent; the smoke account remained at 440 credits |
| 2026-05-29 | Clarified homepage Pro interest record boundary | `da74728` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_browser.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:browser`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; homepage Free/Pro boundary now labels the Pro step as `결제 없이 기록` and says repeated production-package needs are kept as a Pro interest record rather than implying checkout is open. No build pass was spent; the smoke account remained at 440 credits |
| 2026-05-29 | Revalidated source-backed market scan smoke | Current commit | Validation only; no deployment required | `pnpm smoke:market`; production market/competition auto-research returned `mode=openai_web`, `model=gpt-5-mini`, 5 public sources, and 4 competitors/alternatives, so the strict source-backed path is currently healthy without allowing estimate fallback |
| 2026-05-29 | Aligned Pro interest record copy in credit UI | `a63ffa3` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_browser.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:browser`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; guide plan ladder, profile credit boundary, and STEP 5 credit panel now say `Pro 관심 기록` instead of user-facing demand-signal copy, and the STEP 5 production value-path rows are computed outside inline JSX. No build pass was spent; the smoke account remained at 440 credits |
| 2026-05-29 | Folded secondary STEP 5 credit details | `a5e4121` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_billing_credits.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; STEP 5 now keeps the first credit panel focused on production-package value and the immediate action, while Free/Pro criteria live behind `Free/Pro 기준 보기` and spend confidence lives behind `차감 전 확인 보기`. No build pass was spent; the smoke account remained at 440 credits |
| 2026-05-29 | Folded STEP 7 install file details | `4d052fd` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`, `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync`, follow-up `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; STEP 7 now keeps the first view focused on connection file, project-root location, install/check commands, and first task start while folding installed file composition behind `설치 후 생기는 파일 보기`. The full connector lifecycle smoke spent one 30-credit disposable build pass, reducing the smoke account from 470 to 440 credits |
| 2026-05-29 | Folded secondary STEP 8 review details | `e5753ef` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`, `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync`, follow-up `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; STEP 8 now shows `완료/다음/판단` immediately under the one-line outcome, folds the checking order behind `확인 순서 보기`, and folds automatic sync detail rows behind `자동 반영 세부 보기`. The full connector lifecycle smoke spent one 30-credit disposable build pass, reducing the smoke account from 500 to 470 credits |
| 2026-05-29 | Aligned Pro interest record wording | `8ed05ca` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:browser`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; profile action messages, credit shortfall copy, guide FAQ, and user test guide now say `Pro 관심 기록` instead of internal demand/signal wording. The first billing smoke attempt hit local Windows `spawn EPERM`, then passed on standalone rerun |
| 2026-05-29 | Clarified first-use input and output promise | `24e7d2e` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_browser.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 1 first-use guidance now says what to paste and what the user receives before the folded context: `받는 것: 후보 3개, 결과물 형태, 개발 방식`. The first authenticated browser smoke attempt hit local Windows `spawn EPERM`, then passed on standalone rerun |
| 2026-05-29 | Counted Pro interest totals accurately | `9fb58c9` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`, `pnpm smoke:browser:auth`; Pro interest summary now reads the exact accessible total count separately from the recent 100 events used for source/intent/latest-record analysis, so the visible interest count is no longer capped by the recent-events list |
| 2026-05-29 | Made Pro interest summary user-facing | `eddc429` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_billing_credits.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`, `pnpm smoke:browser:auth`; `/profile` now presents Pro interest as `내 Pro 관심 기록`, explains that it records when the user needs Pro without opening checkout, and billing smoke locks the new user-facing copy |
| 2026-05-29 | Allowed no-spend build-sync credit gate smoke | `7b6eb67` | Script/docs only; pushed to `main`; no production deployment required | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm release:check`, `pnpm smoke:build-sync`; default build-sync now verifies invalid TTL and unsupported tool rejection, treats HTTP 402 build-pass enforcement as an expected credit-gate pass, deletes the disposable idea, and does not spend the 30-credit build pass. Full STEP 7/8 connector lifecycle coverage still requires a pre-unlocked smoke idea or `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1` |
| 2026-05-29 | Centralized Pro interest metadata | `a4a1066` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck` after one local Windows `spawn EPERM`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; Pro interest event name, category, plan, 24-hour dedupe window, allowed sources/intents, and labels now live in `src/lib/upgrade-interest.ts` so the server action, profile summary, button prop types, and server reader share one metadata boundary |
| 2026-05-29 | Constrained Pro interest intents | `3068a1e` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; the Pro interest server action now stores only the two supported billing intents, falling back to repeated production packages for unknown input so pre-payment demand data stays labelable in the profile summary |
| 2026-05-29 | Retried billing smoke development route | `c15c618` | Script-only; pushed to `main`; no production deployment required | `node --check scripts/smoke_billing_credits.mjs`, `git diff --check`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`, `pnpm release:check`; billing smoke now retries `/workspace?task=development` once when the direct route briefly renders the login-required screen despite the browser auth cookie, reducing false negatives after fresh deployments |
| 2026-05-29 | Showed Pro interest dedupe rule | `3368d51` | Pushed to `main`; production alias smoke passed after Git integration deploy and billing smoke selector follow-up `3f10f47` | `node --check scripts/smoke_billing_credits.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm quality:full`, `pnpm smoke:prod`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`, `pnpm release:check`; `/profile` Pro interest quality now states that the same account, source, and reason only increments once within 24 hours. The first billing rerun saw the previous production build before propagation; the next rerun exposed a broad `기준:` smoke selector because `중복 기준:` also matched, then `3f10f47` narrowed the selector and billing smoke passed |
| 2026-05-29 | Deduplicated Pro interest signals | `6a542dd` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing` via PowerShell env syntax, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; Pro interest now checks for the same actor/source/intent within 24 hours before inserting a billing telemetry event, so repeated clicks do not inflate pre-payment demand signals. The first billing command used bash-style env syntax in PowerShell and did not run; the first valid billing smoke then hit a transient direct-route session timing issue after deploy and passed on immediate rerun |
| 2026-05-29 | Folded secondary first-use guidance | `e6533f4` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_browser.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 1 now keeps the first-use paste path, input examples, and main AI action visible while moving AI output details, operator role, result-type/development-method split, and saved-output path behind `AI가 만들 결과와 저장 후 흐름 보기`. A local dev smoke attempt was skipped because the local workspace stayed in `Supabase is not configured`; production browser smoke verified the deployed flow |
| 2026-05-29 | Locked workspace credit summary in auth smoke | `a150daf` | Script-only; push to `main`; no production deployment required because the smoke now verifies the already-deployed `/workspace` credit summary | `node --check scripts/smoke_browser_auth.mjs`, `git diff --check`, `pnpm smoke:browser:auth`, `pnpm release:check`; authenticated browser smoke now checks the top `제작 크레딧` summary during direct `/workspace?task=orchestration` entry so the workspace execution-capacity card is protected in the logged-in path |
| 2026-05-29 | Reused workspace credit summary in workbench | `c372013` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `git diff --check`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; `/workspace` now passes the server-read Venture Credit summary into the embedded workbench so STEP 5 has an immediate initial credit state and skips the redundant first client refresh |
| 2026-05-29 | Added STEP 8 sync review summary | `80f92af` | Pushed to `main`; production alias smoke and full build-sync spend smoke passed after selector follow-up `b87488b` | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`, `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync`; STEP 8 automatic sync now repeats `반영 결과`, `다음 작업`, and `최근 확인` under the one-sentence sync summary. The first full spend rerun consumed one 30-credit disposable pass and exposed an over-broad smoke selector for `완료`; `b87488b` narrowed the selector, the second full spend rerun passed, and the follow-up billing smoke confirmed 30 credits remain |
| 2026-05-29 | Showed workspace credit summary | `b5927ae` | Pushed to `main`; production alias smoke passed after Git integration deploy | `node --check scripts/smoke_browser.mjs`, `pnpm typecheck`, `git diff --check`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; `/workspace` header now shows a `제작 크레딧` card linked to `/profile`, with logged-in remaining credits and monthly build-pass capacity or a login/profile fallback, so users see execution capacity before reaching STEP 5 |
| 2026-05-29 | Centralized credit display formatting | `9845a36` | Pushed to `main`; production alias smoke passed after Git integration deploy | `pnpm typecheck`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`, `git diff --check`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 5 and `/profile` now share Venture Credit number formatting, signed ledger formatting, and monthly build-pass capacity helpers from `src/lib/billing.ts` while keeping the visible credit model copy unchanged. Two parallel pnpm smoke attempts hit local Windows `spawn EPERM`, then passed on standalone rerun |
| 2026-05-29 | Revalidated full build-sync credit spend smoke | Current commit | Validation only; no deployment required | `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing` confirmed the disposable smoke account had 120 credits before the rerun; `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync` passed with one 30-credit disposable build-pass spend, verifying registry readiness, Cursor/Codex/Claude Code/Google Antigravity setup files, progress write-back, STEP 7/8 UI, revoke, tamper, expired-token, unsupported-tool, and revoked-token denial paths; the follow-up billing smoke confirmed 90 credits remain |
| 2026-05-29 | Made STEP 8 build-sync smoke state-aware | `ef6fd49` | Pushed to `main`; no production smoke required because this was a smoke assertion fix only | `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing` confirmed 120 credits before reruns; three `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync` attempts each created and cleaned up a disposable smoke idea, progressing into STEP 8 before exposing state-specific smoke assertions: duplicate STEP 7 CTA text, hidden detailed outcome labels before opening the details panel, state-dependent decision options, and `숫자 리포트` vs `상세 리포트`; after the fix, `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm release:check`, and `pnpm quality:full` passed. The reruns consumed the 120-credit allocation, so a final full build-sync rerun requires adding at least 30 credits back to the smoke account |
| 2026-05-29 | Tightened STEP 6 execution bridge smoke selector | `0cfa2d9` | Pushed to `main`; no production smoke required because this was a smoke assertion fix only | `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing` confirmed 30 credits before the rerun; `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync` created and cleaned up a disposable smoke idea but failed on another strict-mode assertion because `연결 파일` appeared in both bridge helper copy and the Cursor final-execution card; after the fix, `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm release:check`, and `pnpm quality:full` passed. The failed spend run consumed the 30-credit disposable build pass, so a full build-sync rerun now requires adding 30 credits back to the smoke account |
| 2026-05-29 | Revalidated source-backed market scan smoke | Current commit | Validation only; no deployment required | `pnpm smoke:market`; production market/competition auto-research returned `mode=openai_web`, `model=gpt-5-mini`, 5 public sources, and 4 competitors/alternatives, so the strict source-backed path is currently healthy without allowing estimate fallback |
| 2026-05-29 | Explained Pro interest reasons on credit shortfall | `f59cbed` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_billing_credits.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`, `pnpm smoke:browser:auth`; STEP 5 now shows `반복 제작`, `외부 도구`, and `시장 근거` as Pro-interest reasons when credits are short while checkout remains closed. A pre-deploy billing smoke failed against the previous production build, and the first authenticated browser smoke after deploy saw a transient Supabase `Failed to fetch` console error; both passed after deployment propagation/retry |
| 2026-05-29 | Tightened STEP 6 build-sync smoke selector | `0723ac9` | Pushed to `main`; no production smoke required because this was a smoke assertion fix only | `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing` confirmed 30 credits before the first spend run; `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync` created and cleaned up a disposable smoke idea but failed on a strict-mode assertion because `T-001` appeared in both helper copy and the task-number cell; after the fix, `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm release:check`, and `pnpm quality:full` passed. The failed spend run consumed the 30-credit disposable build pass, so a full build-sync rerun now requires adding 30 credits back to the smoke account |
| 2026-05-29 | Hoisted STEP 6 action summaries | `83b7d44` | Pushed to `main`; no production smoke required because this was a render-structure refactor with unchanged UI copy and behavior | `git diff --check`, `pnpm typecheck`, `pnpm quality:full`; STEP 6 current-action and first-task-lock row data now live in named render constants instead of inline JSX arrays |
| 2026-05-29 | Highlighted first work-order lock | `ce1091f` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 6 now shows a `첫 제작 범위 잠금` panel with task number, task name, and acceptance criteria so operators can confirm T-001 before moving to final execution. Full build-sync spend smoke remains deferred to avoid consuming a 30-credit disposable build pass |
| 2026-05-29 | Deduplicated build-sync smoke auth reads | `4774166` | Pushed to `main`; no production smoke required because this was a local smoke harness refactor only | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm quality:full`; build-sync smoke now reads the browser Supabase access token through one helper instead of repeating the same browser-side function in disposable idea, launch package, and cleanup paths. Full build-sync spend smoke remains deferred to avoid consuming a 30-credit disposable build pass |
| 2026-05-29 | Showed saved package usage path | `1f8b98a` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_billing_credits.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`, `pnpm smoke:browser:auth`; STEP 5 now shows saved package usage across `STEP 6`, `STEP 7`, and `STEP 8` so the production package reads as an execution path from work order to connection file to learning review. A pre-deploy billing smoke attempt failed against the previous production build, then passed after the new deployment propagated |
| 2026-05-29 | Clarified first-use operator role | `a6537b0` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_browser.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm smoke:browser`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 1 now shows `AI가 먼저`, `사용자는`, and `다음에는` before the input so first-time users understand that AI drafts the materials and the operator confirms one idea, saves, then uses the bottom next-step button |
| 2026-05-29 | Surfaced final execution run location | `ac1b945` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 7 now repeats the execution location at the top of the final execution action banner with `실행할 곳`, `아닌 곳`, and `5초 확인` so users see the project-root rule before the detailed setup cards. Full build-sync spend smoke remains deferred to avoid consuming a 30-credit disposable build pass |
| 2026-05-29 | Clarified STEP 8 decision options | `f105c1a` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 8 judgment candidates now appear as a numbered `오늘 고를 판단 후보` panel instead of loose pills, and build-sync smoke locks the candidate panel copy. The first authenticated browser smoke attempt lost the direct workbench session after deploy, then passed on immediate retry. Full build-sync spend smoke remains deferred to avoid consuming a 30-credit disposable build pass |
| 2026-05-28 | Updated production smoke anchor after homepage hero Koreanization | `e438e60` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `git diff --check`, `pnpm release:check`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; production smoke now checks a stable `landing-credit-model` data-smoke anchor instead of the removed English hero label, while browser smoke owns the Korean copy validation. The first post-hero `pnpm smoke:prod` failed on stale expected text and passed after this update |
| 2026-05-28 | Locked Korean homepage hero labels in browser smoke | `8e3c232` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_browser.mjs`, `git diff --check`, `pnpm release:check`, `pnpm smoke:browser`, `pnpm quality:full`; browser smoke now verifies the Korean homepage hero labels and fails if stale English hero micro-labels return |
| 2026-05-28 | Koreanized homepage hero micro-labels | `0d2de22` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm smoke:browser`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser:auth`, production Playwright desktop/mobile visual QA; homepage hero and motion deck now use Korean operator-facing labels such as AI-first product OS, production package OS, memo, market signal, risk, package handoff, and handoff completeness |
| 2026-05-28 | Removed unused checkout readiness UI code while payments stay paused | `7272ca1` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; the unrendered Stripe readiness panel and env-readiness helper were removed while billing smoke continues to fail if a payment readiness panel appears during the checkout pause |
| 2026-05-28 | Centralized final-execution connection status copy | `5676281` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; repeated STEP 7 automatic sync connection and connection-record fallback copy now comes from shared helpers so all external tools stay aligned |
| 2026-05-28 | Reworded final-execution connection fallback copy | `64a244a` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 7 connection status and fallback messages now say automatic sync connection and connection record setup instead of exposing token-ledger wording in the primary UI |
| 2026-05-28 | Locked product-term package value copy in smoke tests | `2e82c76` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_browser.mjs`, `node --check scripts/smoke_billing_credits.mjs`, `git diff --check`, `pnpm release:check`, `pnpm smoke:browser`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser:auth`; browser and billing smoke now verify product-facing package value copy says product planning document and screen structure instead of PRD abbreviations |
| 2026-05-28 | Reworded package value copy from abbreviations to product terms | `0bc7722` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; homepage, guide, STEP 5 credit panel, profile credit summary, STEP 6 bridge, and hero motion now say product planning document and screen structure instead of PRD/IA in first-facing value copy |
| 2026-05-28 | Clarified shell next-step navigation copy | `8f482d9` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; console shell blocker and focus copy now says saving opens the bottom next-step button instead of implying automatic movement between steps |
| 2026-05-28 | Added STEP 5 save-to-execution path cue | `ae08057` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_billing_credits.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; STEP 5 now explicitly says that saving opens STEP 6 work-order review and then STEP 7 connection-file download, reducing the chance users look for downloads in the package step |
| 2026-05-28 | Hoisted profile Pro conversion boundary items | `3aa6bb9` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; `/profile` Pro conversion boundary copy now lives in a named array before rendering while checkout remains paused and Pro interest remains the only conversion action |
| 2026-05-28 | Hoisted STEP 7 final execution guide arrays | `beba7bb` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 7 simple path, install result, and command path items now live outside the JSX block while the user-facing final execution guide remains unchanged |
| 2026-05-28 | Optimized STEP 8 and profile credit layout | `0121cac` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 8 action ladder items now live outside the render body and `/profile` credit summary grids use three columns for three cards instead of leaving an empty fourth slot. The first `pnpm quality:full` attempt timed out at 300s without output; the immediate retry passed with a longer timeout |
| 2026-05-28 | Added profile credit next-action cue | `a1b91ba` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_billing_credits.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; `/profile` Venture Credits now shows a `지금 할 일` cue that tells users whether to open a STEP 5 build pass or leave Pro interest while checkout remains paused |
| 2026-05-28 | Added STEP 8 action ladder | `c390534` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 8 now shows `1. 완료 확인`, `2. 다음 하나`, `3. 판단 하나` before the detailed report so users can make one decision without reading every card; full build-sync spend smoke remains deferred to avoid consuming a 30-credit disposable build pass. First parallel `pnpm smoke:browser` attempt hit a local Windows `spawn EPERM`, then passed on immediate standalone retry |
| 2026-05-28 | Added STEP 7 install result preview | `11a1acf` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 7 now shows that the setup creates a START file, task list, and progress record before the user runs the connection file; full build-sync spend smoke remains deferred to avoid consuming a 30-credit disposable build pass |
| 2026-05-28 | Clarified first-user development method wording | `9d626f0` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_browser.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; homepage and `/guide` now say AI also separates the development method from the result type, and browser smoke locks a concrete `모바일 앱으로 만들고 Cursor로 개발합니다` example |
| 2026-05-28 | Added STEP 1 generated-slot development method cues | `ff5188d` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; AI-generated STEP 1 candidate slots now show the selected development method next to result type before extraction/save |
| 2026-05-28 | Hoisted STEP 5 static package arrays | `60b9c34` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 5 static current-action, execution-brief, and package-value arrays now live outside the component render body while UI behavior stays unchanged |
| 2026-05-28 | Added STEP 8 single-decision rule | `994b8ea` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 8 now tells users to choose only one judgment candidate before opening detailed reports; full build-sync spend smoke remains deferred to avoid consuming a 30-credit disposable build pass |
| 2026-05-28 | Added STEP 7 folder check question | `9b88116` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 7 now asks users to confirm the current terminal folder contains `package.json`, `app`, or `src` before running the setup command; full build-sync spend smoke remains deferred to avoid consuming a 30-credit disposable build pass |
| 2026-05-28 | Added STEP 5 package current-action cue | `7d7863f` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_billing_credits.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; STEP 5 now leads with a current-action cue that says AI makes the execution criteria, the operator reviews the summary, saves it, and moves to STEP 6 while downloads stay in STEP 7 |
| 2026-05-28 | Added STEP 1 candidate development method cues | `f52d242` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 1 recommended, queued, and detailed candidate cards now keep the selected development method visible beside result type before saving |
| 2026-05-28 | Aligned user guide completion checklist with STEP 6 | `d8c0cfe` | Docs only; no deployment required | `git diff --check`, `pnpm release:check`; user guide completion criteria now says STEP 5 package save flows through STEP 6 work-order review before STEP 7 final execution |
| 2026-05-28 | Hoisted STEP 1 first-use static arrays | `3befed3` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 1 static first-use fast path and build-choice split data now live outside the component render body while the UI stays unchanged |
| 2026-05-28 | Added STEP 1 build choice split | `9844651` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_browser.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 1 now shows `무엇을 만들지` as result type and `어떻게 만들지` as development method before the input box, and browser smoke locks the split |
| 2026-05-28 | Removed STEP 8 auto navigation controls | `b12d711` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 8 now keeps report copy as the only execution button and replaces STEP 7 auto-navigation controls with static guidance, including the empty state; full build-sync spend smoke remains deferred to avoid consuming a 30-credit disposable build pass |
| 2026-05-28 | Folded STEP 8 detailed judgment cards | `958ee94` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 8 now keeps detailed completed/remaining/judgment cards behind `판단 근거 자세히 보기` so the first view stays focused on one-line outcome, current action, and next judgment; full build-sync spend smoke remains deferred to avoid consuming a 30-credit disposable build pass |
| 2026-05-28 | Added STEP 6 execution bridge | `2a2e22e` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 6 now explains how the saved production package becomes a work order and then a final execution connector file; full build-sync spend smoke remains deferred to avoid consuming a 30-credit disposable build pass |
| 2026-05-28 | Added first-use AI output preview | `3440c62` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_browser.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 1 now tells first-time users that AI 정리 returns idea candidates, product surface, development method, and the first validation question before save |
| 2026-05-28 | Added STEP 8 judgment question | `5fafa1f` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 8 now shows the exact question the operator should answer before opening detailed reports; full build-sync spend smoke remains deferred to avoid consuming a 30-credit disposable build pass |
| 2026-05-28 | Added STEP 7 tool start mode cue | `f2bc1b1` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 7 now shows whether the selected external tool starts in an IDE or a terminal agent before the detailed setup guide; full build-sync spend smoke remains deferred to avoid consuming a 30-credit disposable build pass |
| 2026-05-28 | Added STEP 5 execution package brief | `ec42b6e` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_billing_credits.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; STEP 5 now names the saved package as a production start package and surfaces first message, first task, and final execution connection files before the detailed generation flow |
| 2026-05-28 | Paused profile payment readiness UI | `18cb228` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_billing_credits.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; `/profile` now stays focused on Venture Credits and Pro interest while checkout/provider choice is deferred, and billing smoke fails if the Stripe readiness panel is visible during the pause |
| 2026-05-28 | Clarified STEP 8 primary action labels | `4d42fe1` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 8 now labels its primary action as `다음 작업 보러가기`, `최종 실행 확인`, or `리포트 복사` based on what the user will actually do next; full build-sync spend smoke remains deferred to avoid consuming a 30-credit disposable build pass |
| 2026-05-28 | Added STEP 7 first-task start cue | `c3771f8` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy remained blocked by local certificate-chain trust | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 7 simple execution path now includes `첫 작업 시작` after install/check so users see that the selected tool's START file should be used as the first agent message before T-001 begins; full build-sync spend smoke remains deferred to avoid consuming a 30-credit disposable build pass |
| 2026-05-28 | Added first-use input examples | `da8e72e` | Pushed to `main`; Git integration production alias verified after direct Vercel CLI deploy was blocked by local certificate-chain trust | `git diff --check`, `node --check scripts/smoke_browser.mjs`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, local browser smoke against `http://127.0.0.1:3102`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 1 now shows non-interactive examples for meeting notes, GPT conversations, and automation work before the input box so first-time users know what can be pasted without adding extra execution buttons |
| 2026-05-28 | Recorded payment entitlement security gate | `085fc5c` | Docs only; no deployment required after previous production deployment `dpl_8kYPSmUC3QyMSjFZhNcAEvHgJv99` | `git diff --check`, `pnpm release:check`; `docs/RISK_REGISTER.md` now records premature checkout/client-side Pro entitlement as a high billing/security risk, and `docs/SECURITY_PRIVACY.md` now requires hidden secret values, verified Stripe webhooks, server-side entitlement writes, and Free retention for failed/canceled checkout |
| 2026-05-28 | Added profile Stripe readiness panel | `4344889` | Pushed to `main`; production deployment `dpl_8kYPSmUC3QyMSjFZhNcAEvHgJv99` ready | `node --check scripts/smoke_billing_credits.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; `/profile` now shows Stripe checkout readiness by required env names and counts only, without exposing secret values, while checkout remains off until server checkout, verified webhooks, and entitlement writes exist |
| 2026-05-28 | Added Pro interest signal quality summary | `d22248b` | Pushed to `main`; production deployment `dpl_EWKFstPiUNH8JeBPZzY7oJT6zcbc` ready | `node --check scripts/smoke_billing_credits.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; `/profile` now labels Pro interest signal quality as no-signal, needs-more-signal, or price-test-ready before checkout exists, and billing smoke verifies the new signal-quality selector without spending credits |
| 2026-05-28 | Centralized credit and Pro plan copy constants | `255b5a3` | Pushed to `main`; production deployment `dpl_F59xZZJJjKCVsJ5avZyLmgJ4FBQF` ready | `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; credit amount, build-pass cost, package artifact counts, Pro upgrade value copy, and the Free -> build pass -> Pro ladder now come from `src/lib/billing.ts` so homepage, guide, profile, STEP 5, and build-sync enforcement copy stay in sync |
| 2026-05-28 | Added guide Free-to-Pro plan ladder | `c45b156` | Pushed to `main`; production deployment `dpl_56cx89gs9kdvhmi5Y4YhRP5qoxdf` ready | `node --check scripts/smoke_browser.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; `/guide` now shows a Free -> build pass -> Pro interest ladder so users can distinguish first validation, one-idea execution, and repeated production demand before checkout exists |
| 2026-05-28 | Added external sync result summaries | `57f2f12` | Pushed to `main`; production deployment `dpl_6EXABvfg9is8L1Qw34pcd67ouEu3` ready | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 7 now shows `반영 결과`, `다음 작업`, and `최근 확인` in the automatic sync area, STEP 8 shows an `자동 반영 요약` sentence before detailed reports, and build-sync selector coverage was updated without rerunning the opt-in 30-credit spend smoke |
| 2026-05-28 | Normalized market-scan public source citations | `9538ff3` | Pushed to `main`; production deployment `dpl_75DwkrHwamsfMcNcBsv9XVbMuzSq` ready | `node --check scripts/smoke_market_scan.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `pnpm smoke:market`; market scan now normalizes public HTTP(S) source URLs, rejects local/private/user-input fallback citations, dedupes by normalized URL, keeps the highest-quality duplicate, and strict market smoke passed with `mode=openai_web`, `model=gpt-5-mini`, 5 sources, and 5 competitors |
| 2026-05-28 | Added Pro plan-fit conversion cues | `84c471f` | Pushed to `main`; production deployment `dpl_7CnDbK2xXP5Brai2MyNuYFrEiqqf` ready | `node --check scripts/smoke_billing_credits.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; STEP 5 and `/profile` now show Free monthly build-pass capacity, current remaining pass fit, when Pro becomes useful, and the immediate action before any Stripe checkout is enabled; billing smoke verified profile conversion selectors and STEP 5 Pro path without spending credits |
| 2026-05-28 | Simplified STEP 8 next-judgment review | `0151dd3` | Pushed to `main`; production deployment `dpl_BxzL5EDBPJT7pGLXT1E5wC1pMheW` ready | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 8 now expands the primary `완료`, `다음`, `판단` review with short explanations and adds a `다음 판단` line so users can decide what to do before opening detailed reports; full build-sync spend smoke remains an opt-in 30-credit disposable pass rerun |
| 2026-05-28 | Clarified STEP 7 project-root execution check | `877718f` | Pushed to `main`; production deployment `dpl_Haoeu5zmPyAzH1E3m826sqaaXyre` ready | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 7 now adds an `실행 위치 확인` strip that tells users to run the connection file in the external project root, not Downloads, and to confirm the location by seeing real app files such as `package.json`, `app`, or `src`; full build-sync spend smoke was skipped because the latest completed run already passed and each rerun consumes one 30-credit disposable build pass |
| 2026-05-28 | Clarified STEP 5 execution package value | `2f04e0a` | Pushed to `main`; production deployment `dpl_8vQ25SgUxiKrxcW1ZBauTBzHYGQM` ready | `node --check scripts/smoke_billing_credits.mjs`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing` after deployment, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; STEP 5 credit panel now first separates `Free`, `제작 패스`, and `최종 실행` so users see the difference between free judgment materials and the 30-credit execution package before reading detailed package contents |
| 2026-05-28 | Simplified first-use STEP 1 intake path | `43c5ad6` | Pushed to `main`; production deployment `dpl_23d1cFY3aw1G2g2xvMwqf7oAKPPu` ready | `node --check scripts/smoke_browser.mjs`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; first-use STEP 1 now leads with `붙여넣기 -> AI 정리 -> 저장 후 열림`, moves the input box higher, keeps the result-type/development-method example visible, and browser smoke protects the simplified first-run path |
| 2026-05-28 | Completed full build-sync credit spend smoke | `b4a0a65` | No deployment required; production deployment `dpl_FAjFUbEJMkc5uoqLSTK7U7Zyg3SC` remained ready | `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing` confirmed the disposable smoke account had 30 credits, `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync` passed with a 30-credit disposable build-pass spend, and a follow-up `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing` confirmed the account returned to 0 credits; the smoke verified registry ready, Cursor/Codex/Claude Code/Google Antigravity token issuance, progress write-back, STEP 7 final execution UI, STEP 8 task sync, connector setup downloads, hidden generic MCP, revoke, invalid TTL, unsupported tool, cross-idea tamper, expired token, and revoked-token denial paths |
| 2026-05-28 | Kept STEP 6 first task code visible | `258c4b7` | Pushed to `main`; production deployment `dpl_FAjFUbEJMkc5uoqLSTK7U7Zyg3SC` ready | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; STEP 6 current action now keeps the `T-001` task code visible beside the first task title even after external progress creates a real implementation task; billing smoke confirms the disposable smoke account is back at 0 credits, so the final full spend rerun still needs a fresh 30-credit build pass |
| 2026-05-28 | Added STEP 5 build-pass spend confidence cues | `3efeedc` | Pushed to `main`; production deployment `dpl_CTdniEb7yYzp7tbM6ZrVDVWFpoZy` ready | `node --check scripts/smoke_billing_credits.mjs`, `pnpm typecheck`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; STEP 5 now tells the operator that a build pass is recorded to the selected idea, shows the after-spend credit state, and explains that the saved package continues into work order and final execution; billing smoke now checks the STEP 5 credit panel after deployment |
| 2026-05-28 | Centralized workspace task URL parsing | `07c4fea` | Pushed to `main`; production deployment `dpl_2ea4FSomtt9docut83X22WChPhgj` ready | `pnpm typecheck`, `pnpm smoke:browser:auth`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; workbench task IDs now live in `src/lib/workbench-tasks.ts`, `/workspace?task=...` uses the shared `isWorkbenchTask` guard, and direct task links include `decision` and `orchestration` without a separate route allowlist |
| 2026-05-28 | Added authenticated direct STEP 6 route smoke | `37f41fc` | Script/docs only; no deployment required | `node --check scripts/smoke_browser_auth.mjs`, `pnpm smoke:browser:auth`; authenticated browser smoke now waits for the auth cookie and verifies `/workspace?task=orchestration` reaches the STEP 6 current-action strip instead of falling back to score or login |
| 2026-05-28 | Restored direct STEP 6 orchestration links | `a5b4a0f` | Pushed to `main`; production deployment `dpl_Bee5NvQSN17qAoV6vx8EB2VzBfvH` ready | `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, direct Playwright smoke for `/workspace?task=orchestration`; `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing` confirmed the trusted smoke top-up reached 30 credits before the spend attempt, then 0 credits after the disposable build-pass attempt consumed one 30-credit pass; `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync` exposed the missing `orchestration` URL allowlist before the rerun credit balance was exhausted |
| 2026-05-27 | Clarified production package credit value | `58a525b` | Pushed to `main`; production deployment `dpl_3gP8yZ239868a6J72dkZCoXTAMBX` ready | `node --check scripts/smoke_billing_credits.mjs`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:billing`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; profile and STEP 5 now frame the 30-credit build pass as opening an execution package, not buying a document bundle |
| 2026-05-27 | Clarified STEP 1 build contract example | `61fce1b` | Pushed to `main`; production deployment `dpl_Coqxi3XJAhQHGuonDYNGnFB9DtCk` ready | `node --check scripts/smoke_browser.mjs`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; first-use intake now separates `결과물 형태` from `개발 방식` with the example `모바일 앱으로 만들고, Cursor로 개발합니다.`, and browser smoke protects the contract |
| 2026-05-27 | Added STEP 8 one-sentence outcome | `d78fefb` | Pushed to `main`; production deployment `dpl_FqWHf3sQNAFBBemU7tQFz5S7aFip` ready | `node --check scripts/smoke_build_sync_tokens.mjs`, `git diff --check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; STEP 8 now shows a `한 줄 결론` above the outcome cards so the operator sees the next judgment before reading reports, and build-sync smoke will protect the selector when the credit blocker is cleared |
| 2026-05-27 | Hardened Venture Credit RPC amount and period boundaries | Current commit | SQL migration/docs only; production SQL application required before the database boundary is closed | `git diff --check`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:billing`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; direct smoke credit top-up was not performed because local/Vercel env readback does not expose the Supabase admin key and authenticated RPC parameter tricks must not be used as an operator shortcut; `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync` remains blocked before spend with the known 10/30 credit shortfall |
| 2026-05-27 | Added STEP 6 current-action strip | `45aa04a` | Pushed to `main`; production deployment `dpl_8gxVRZSmwwU6XQuF49VaC7f4xWBc` ready | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync` blocked before spend with the known 10/30 credit shortfall, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; STEP 6 now surfaces `지금 할 일`, `첫 작업`, and `다음 단계` above the work-order board, and build-sync smoke will protect the selector when the smoke account credit blocker is cleared |
| 2026-05-27 | Added AI route burst guard | `282c0ac` | Pushed to `main`; production deployment `dpl_7z3acrZvoevyYkDQU1xBwwq1QQ8q` ready | `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:market`; OpenAI-backed idea extraction, sample generation, and market scan routes now share a thin per-client in-memory rate limit so controlled beta keeps first-use fallback behavior while reducing accidental anonymous burst spend |
| 2026-05-27 | Added final execution connection health summary | `5a18cd6` | Pushed to `main`; production deployment `dpl_13PJCG8n84RXq47RH9MxL5xbhRJu` ready | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync` blocked before spend with the known 10/30 credit shortfall, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; STEP 7 connection management now shows automatic write-back status and the recovery path before the detailed connection list, while build-sync smoke protects the selector for the next unblocked full run |
| 2026-05-27 | Added market scan decision strip | `1d57456` | Pushed to `main`; production deployment `dpl_7j8xBodtTeRTKkw2BABHznpYVWia` ready | `node --check scripts/smoke_market_scan.mjs`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:market`; STEP 3 market scan results now surface `지금 판단`, `다음 행동`, and `주의` before detailed market signals, competitors, barriers, and source lists, and market smoke protects `caveat` for the decision strip |
| 2026-05-27 | Added first-use downstream output path | `800bf8e` | Pushed to `main`; production deployment `dpl_5VPaGidjnyjwgj5qvvf1HERyakTm` ready | `node --check scripts/smoke_browser.mjs`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; STEP 1 input now tells first-time users that saving opens business review, risk, validation plan, production package, and external development-tool handoff materials |
| 2026-05-27 | Added credit shortfall upgrade cues | `69ada5a` | Pushed to `main`; production deployment `dpl_7RS1qo84skarVkG2eq7vPcKHSVqv` ready | `node --check scripts/smoke_billing_credits.mjs`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:billing`, `BILLING_SMOKE_ALLOW_AUTH_GRANT=1 pnpm smoke:billing`; profile and STEP 5 now show the exact shortfall to the next production build pass and connect that state to Pro interest without opening checkout |
| 2026-05-27 | Added build-sync credit preflight diagnostics | Current commit | Script/docs only; push to `main` after verification | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm release:check`, `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync` blocked before spend with `10 credits`, `30` cost, and `20` shortfall; build-sync smoke now reads the authenticated credit summary before an opt-in disposable build-pass spend and reports balance, build-pass cost, and shortfall before attempting any spend |
| 2026-05-27 | Hardened source-backed market scan request and source filtering | `0be2a31` | Pushed to `main`; production deployment `dpl_E97qY7nsP73rZgiHZsoqt2PZVoQu` ready | `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:market`; market scan API now uses the hosted web search tool as a required path, records clearer parse fallback reasons, filters `openai_web` source lists down to public HTTP(S) references instead of user-input placeholders, and strict smoke passed with `mode=openai_web`, `model=gpt-5-mini`, 5 sources, and 5 competitors |
| 2026-05-27 | Documented Stripe payment setup boundary | Current commit | Docs/env example only; no deployment required beyond push | `pnpm release:check`; added Stripe env placeholders and setup boundary so checkout stays off until server-side sessions, verified webhooks, and entitlement writes are implemented |
| 2026-05-27 | Added profile Free/Pro conversion boundary | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_billing_credits.mjs`, `pnpm quality:full`; `/profile` now shows the current Free value, Pro value, and current action before the Pro interest button so payment demand stays clear without opening checkout |
| 2026-05-27 | Added first-use intake one-sentence guide | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_browser.mjs`, `pnpm quality:full`; STEP 1 now tells new users to paste the memo and press AI organize, while smoke protects the first-use instruction before the input examples |
| 2026-05-27 | Added STEP 8 simple review strip | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm quality:full`; STEP 8 now repeats the essential `완료`, `다음`, `판단` summary inside the primary action band so users can understand the outcome before opening task evidence or operator reports |
| 2026-05-27 | Added STEP 7 simple execution mode | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm quality:full`; final execution now leads with `실행만 하기`, shows file receive, project-root move, install/check commands as the primary path, and folds tool-specific IDE/agent details behind a `자세히 보기` guide while build-sync smoke verifies the simplified selectors |
| 2026-05-27 | Clarified STEP 5 build-pass value path | Current commit | Push to `main`, then production smoke after deployment | `pnpm typecheck`, `pnpm quality:full`; STEP 5 now explains the 30-credit build pass as a 3-step value path: open all 10 production materials, save the AI package, then receive external-tool connection files |
| 2026-05-27 | Added profile Pro interest summary | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_billing_credits.mjs`, `pnpm typecheck`, `pnpm quality:full`; `/profile` now summarizes visible Pro interest signals by total count, source, intent, latest event, and account count using the existing telemetry RLS boundary |
| 2026-05-27 | Added profile credit ledger history | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_billing_credits.mjs`, `pnpm typecheck`, `pnpm quality:full`; `/profile` now shows current-period credit grant, current-period spend, and recent credit ledger entries, and billing smoke checks the authenticated ledger array/panel without spending credits |
| 2026-05-27 | Added guide Pro interest FAQ | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_browser.mjs`, `pnpm typecheck`, `pnpm quality:full`; `/guide` now tells users where Pro interest can be registered without starting checkout, and browser smoke protects the FAQ wording |
| 2026-05-27 | Added STEP 8 decision options | Current commit | Push to `main`, then production smoke after deployment | `pnpm typecheck`, `pnpm quality:full`; STEP 8 now shows state-aware judgment options under the primary action so users can choose whether to keep building, connect signals, approve the next build, or pause without opening reports first |
| 2026-05-27 | Added homepage Free/Pro/Team upgrade boundary | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_browser.mjs`, `pnpm typecheck`, `pnpm quality:full`; homepage now explains Free for first validation, Pro for repeated production and external tool execution, and Team for shared workflows before signup |
| 2026-05-27 | Added STEP 5 insufficient-credit Pro interest path | Current commit | Push to `main`, then production smoke after deployment | `pnpm typecheck`, `pnpm quality:full`; STEP 5 now reuses the Pro interest button when credits are too low for the next build pass, recording a billing telemetry event without starting checkout |
| 2026-05-27 | Added pre-payment Pro interest signal | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_billing_credits.mjs`, `pnpm quality:full`; `/profile` now lets logged-in users register Pro interest without starting payment, storing an `upgrade_interest_clicked` billing telemetry event while billing smoke only checks button visibility |
| 2026-05-27 | Aligned guide credit FAQ with homepage pricing boundary | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_browser.mjs`, `pnpm quality:full`; `/guide` now explains when the 30-credit build pass is used, and browser smoke protects the credit FAQ wording |
| 2026-05-27 | Added homepage Venture Credits boundary | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_browser.mjs`, `pnpm quality:full`; public home now shows the beta credit model before signup: Free 100 credits, a 30-credit build pass, and the Free 4/10 production-material boundary |
| 2026-05-27 | Added next-action guidance to saved idea lists | Current commit | Push to `main`, then production smoke after deployment | `pnpm quality:full`; `/workspace/ideas` cards now show the next action for each saved idea and the primary button says `다음 행동 열기`, reducing recall work when users return to continue an idea |
| 2026-05-27 | Added profile upgrade trigger guidance | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_billing_credits.mjs`, `pnpm quality:full`; `/profile` now explains that Free is for first validation and Pro value starts when the user needs repeated production packages, external tool write-back, and source-backed market evidence |
| 2026-05-27 | Added credit value and remaining build-pass summaries | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_billing_credits.mjs`, `pnpm quality:full`; `/profile` and STEP 5 now show remaining monthly build-pass capacity and explain that one build pass opens PRD, screen structure, design direction, technical direction, work order, and external tool handoff files |
| 2026-05-27 | Simplified STEP 1 recommended-idea next action | Current commit | Push to `main`, then production smoke after deployment | `pnpm quality:full`; after AI organizes a memo, the recommendation card now says `AI가 먼저 고른 아이디어` and tells the operator to confirm one idea and save to open the next step, while the edit action is framed as optional |
| 2026-05-27 | Clarified STEP 7 project-root execution copy | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm quality:full`; final execution now says the operator only needs to receive the connection file, move it to the real project root, run the install command, and run the check command, with remaining internal English labels replaced by Korean execution labels |
| 2026-05-27 | Optimized STEP 8 telemetry summaries and Koreanized remaining operator labels | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm quality:full`; STEP 8 now reuses memoized product-event counts/window counts for funnel and learning cards, and remaining English labels such as `execution signal` now appear as user-facing Korean labels |
| 2026-05-27 | Simplified empty-workspace first action | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_browser.mjs`, `pnpm quality:full`; when no idea is selected, the empty state now says to paste notes and uses `메모 붙여넣고 AI 정리하기` to focus STEP 1 instead of reopening the same workspace URL |
| 2026-05-27 | Enabled production credit enforcement for external tool connections | Current commit | Vercel Production env `ENFORCE_CREDIT_BUILD_PASS=1` applied by operator and redeployed | `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:billing`, `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync`, `pnpm smoke:browser`, authenticated `pnpm smoke:billing`; build-sync token smoke confirmed `Credit build pass: unlocked for disposable smoke idea`, so external-tool connection tokens are now server-gated by the 30-credit build pass |
| 2026-05-27 | Prepared build-sync smoke for credit enforcement | Current commit | Script/docs only; push to `main` after verification | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm release:check`, `pnpm smoke:build-sync`; build-sync smoke can now spend one disposable 30-credit build pass only when `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1` is explicitly set, so `ENFORCE_CREDIT_BUILD_PASS=1` can be verified without using a real operator idea |
| 2026-05-27 | Simplified STEP 5 credit next action copy | Current commit | Push to `main`, then production smoke after deployment | `pnpm quality:full`; STEP 5 credit panel now shows a single `지금 할 일` sentence that mirrors the current build-pass/button state |
| 2026-05-27 | Added profile Venture Credits summary | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_billing_credits.mjs`, `pnpm typecheck`, `pnpm quality:full`; `/profile` now reads the shared server credit summary and shows monthly credits, 30-credit build pass cost, Free 4/10 package boundary, and opened pass count before the operator reaches STEP 5 |
| 2026-05-27 | Added Venture Credits build-pass pilot | Current commit | Push to `main`, then production smoke after deployment; credit migration remains a user-applied Supabase SQL action | `pnpm typecheck`; STEP 5 now shows Free 100 monthly credits, 30-credit production-package unlock, Free 4/10 package boundary, deploy-safe missing-migration fallback, and billing negative-path smoke |
| 2026-05-27 | Added source-backed market scan summary cards | Current commit | Push to `main`, then production smoke after deployment | `pnpm quality:full`; STEP 3 market scan results now show investigation mode, source count, and competitor/substitute count before detailed evidence lists |
| 2026-05-27 | Drafted Free/Pro/Team monetization model | Current commit | Docs-only; push to `main` after verification | `pnpm release:check`; monetization model frames paid value around source-backed market scans, production package export, external tool sync, and team workflows while keeping first idea understanding and safety guidance useful |
| 2026-05-27 | Renamed STEP 8 telemetry adapter copy to outcome-signal language | Current commit | Push to `main`, then production smoke after deployment | `pnpm quality:full`; STEP 8 advanced connection controls now say `성과 신호 연결 가이드` and keep API details behind the existing collapsed panel |
| 2026-05-27 | Separated STEP 1 result type and development method in direct intake | Current commit | Push to `main`, then production smoke after deployment | `pnpm quality:full`; direct-intake copy now says AI is sorting idea, result type, and development method, and the manual preview shows `결과물 형태` and `개발 방식` as separate cards before saving |
| 2026-05-27 | Restored and tightened source-backed market smoke | Current commit | Script/docs only; push to `main` after verification | `pnpm smoke:market` passed after OpenAI quota/billing key rotation with `mode=openai_web`, `model=gpt-5-mini`, 5 sources, and 5 competitors; smoke now requires public source URLs, non-fallback source types, source reasons, stronger competitor fields, market signals, barriers, and research queries |
| 2026-05-26 | Clarified STEP 5 production package purpose | Current commit | Push to `main`, then production smoke after deployment | `pnpm lint`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`; STEP 5 now names execution package, scope lock, and next-step connection before detailed generated documents |
| 2026-05-26 | Added first-use STEP 1 input/output/role hints | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_browser.mjs`, `pnpm lint`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`; STEP 1 now shows what to paste, what AI creates, and what the operator does before the textarea |
| 2026-05-26 | Added STEP 7 external project-root execution path cards | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm lint`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`; final execution now shows receive, external project root, and verify-command steps before connector details |
| 2026-05-26 | Reduced repeated implementation task progress scans | Current commit | Push to `main`, then production smoke after deployment | `pnpm lint`, `pnpm typecheck`, `pnpm quality:full`; shared task progress stats now feed STEP 6/8 counts, launch readiness, and left-rail development status |
| 2026-05-26 | Compressed STEP 8 into completed/remaining/decision review | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `pnpm release:check`, local `BROWSER_SMOKE_URL=http://localhost:3007` `pnpm smoke:browser`, `pnpm quality:full`; local build-sync smoke reached `/login` but timed out waiting for localhost auth cookie, so production `pnpm smoke:build-sync` is required after deploy |
| 2026-05-26 | Clarified first-use result type, development method, and STEP 7/8 action copy | Current commit | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_browser.mjs`, `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `pnpm quality:full`, local `BROWSER_SMOKE_URL=http://localhost:3007` with extended timeout `pnpm smoke:browser` after one dev-server `networkidle` timeout |
| 2026-05-26 | Fixed build-sync endpoint generation to avoid protected Vercel branch URLs | `7f7e18b` | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`; local Cursor test folder endpoint patched to production URL |
| 2026-05-26 | Clarified Cursor Workspace MCP manual activation step | `30d471d` | Push to `main`, then production smoke after deployment | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full` |
| 2026-05-26 | Clarified first Cursor terminal command and guarded downloaded connector CLI syntax | `71cc3d6` | Pushed to `main`; production updated and smoke passed | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:build-sync`, `pnpm smoke:browser` |
| 2026-05-25 | Added unsupported build-sync tool denial smoke | `e0f9193` | Script/docs only; pushed to `main` after verification | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm release:check`, `pnpm smoke:build-sync`; smoke now confirms `generic_mcp` token requests return HTTP 400 while generic MCP remains deferred |
| 2026-05-25 | Added invalid build-sync token TTL denial smoke | `cc99fdb` | Script/docs only; pushed to `main` after verification | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm release:check`, `pnpm smoke:build-sync`; smoke now confirms negative `expiresInSeconds` token requests return HTTP 400 before any connection is issued |
| 2026-05-25 | Added browser smoke coverage for STEP 6 guide sequencing | `78e148f` | Script-only; pushed to `main` after verification | `node --check scripts/smoke_browser.mjs`, `pnpm release:check`, `pnpm smoke:browser`; browser smoke now protects the `/guide` wording that STEP 6 work-order review comes before STEP 7 external connection files |
| 2026-05-25 | Aligned the public guide page with STEP 6 before final execution | `5087c11` | Pushed to `main`; production smoke passed after deployment | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; `/guide` now says the production package is saved first, STEP 6 confirms the generated work order, and STEP 7 installs the selected external tool connection file |
| 2026-05-25 | Clarified STEP 6 work-order placement in the user test guide | `343c162` | Docs-only; pushed to `main` after verification | `pnpm release:check`; quick success path now says STEP 5 saves the production package, STEP 6 confirms the generated work order, and STEP 7 handles final external-tool connection files |
| 2026-05-25 | Added expired build-sync token denial smoke | `99ca3b2` | Pushed to `main`; production smoke passed after deployment | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:build-sync`; build-sync smoke now verifies expired-token rejection in addition to allowed, cross-idea denied, revoked denied, and four named connector setup checks |
| 2026-05-25 | Removed stale AdMate-as-canonical runtime wording | `8b79320` | Docs-only; pushed to `main` after verification | `pnpm release:check`; beta, CI, and RLS boundary docs now name `D:\Codex\venture-lab` as the canonical AI Venture Lab product source and keep AdMate paths read-only reference only |
| 2026-05-25 | Aligned Supabase setup guide with build-sync denied-case smoke | `a42247a` | Docs-only; pushed to `main` after verification | `pnpm release:check`; Supabase setup now mentions the tampered cross-idea token rejection covered by `pnpm smoke:build-sync` |
| 2026-05-25 | Simplified STEP 6 guidance around generated work order review | `6c2d86a` | Pushed to `main`; production smoke passed | `pnpm lint`, `pnpm typecheck`, `pnpm release:check`, `pnpm smoke:browser`, `pnpm smoke:prod`, `pnpm smoke:routes`; STEP 6 side guidance now tells users to review the generated order and continue, with status changes framed as optional tracking only |
| 2026-05-25 | Added cross-idea build-sync token denial smoke | `f26b7dc` | Script/docs only; pushed to `main` after verification | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm smoke:build-sync`; smoke now tampers a live token to another idea id and expects HTTP 401 before revoke checks |
| 2026-05-25 | Added first-step progress regression smoke | `a326adf` | Script-only; pushed to `main` after verification | `node --check scripts/smoke_browser.mjs`, `pnpm smoke:browser`; browser smoke now fails if STEP 1 returns to the old `진행 1/8` counter instead of showing `완료 0/8` before any saved step |
| 2026-05-25 | Added STEP 8 task-led smoke assertions | `73485ed` | Script-only; pushed to `main` after verification | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm smoke:build-sync`; smoke now protects the STEP 8 order of reading the current action, task progress list, and only opening the operator report when needed |
| 2026-05-25 | Clarified the STEP 1 candidate replay button copy | `02c865d` | Pushed to `main`; production smoke passed | `pnpm lint`, `pnpm typecheck`, `pnpm release:check`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; secondary AI review action now says `빠진 후보 다시 확인` instead of the more operator-like `누락된 후보 점검` |
| 2026-05-25 | Clarified the user test path for external connector setup downloads | `d398f39` | Docs-only; pushed to `main` | `pnpm release:check`; user test guide now checks that the selected tool downloads the matching `*-setup.ps1` file before project-root install |
| 2026-05-25 | Added setup-file download assertions to the external connector smoke | `c3df5df` | Script-only; pushed to `main` | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm smoke:build-sync`, `pnpm release:check`; smoke now verifies the downloaded `.ps1` setup files for Cursor, Codex, Claude Code, and Google Antigravity include the expected project-root files |
| 2026-05-25 | Removed stale Cursor-only handoff copy from external tool messaging | `886fc0b` | Pushed to `main`; production smoke passed | `pnpm lint`, `pnpm typecheck`, `pnpm smoke:build-sync`, `pnpm release:check`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; Cursor, Codex, Claude Code, and Google Antigravity now stay framed as the supported automatic sync paths |
| 2026-05-25 | Made external progress backup messages follow the selected build tool | `396b8b8` | Pushed to `main`; production smoke passed | `pnpm lint`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:build-sync`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; backup progress import now names Cursor, Codex, Claude Code, or Google Antigravity instead of falling back to Cursor-only text |
| 2026-05-25 | Aligned internal handoff docs with the four named external connectors | `19989ac` | Docs-only; pushed to `main`, production smoke passed | `pnpm release:check`, `pnpm smoke:prod`; AI-first flow, user guide, and Cursor-specific guide now describe Cursor, Codex, Claude Code, and Google Antigravity as live named connector paths |
| 2026-05-25 | Updated the public guide from Cursor-only wording to the four supported external tools | `1f840ba` | Pushed to `main`; production smoke passed | `node --check scripts/smoke_browser.mjs`, `pnpm lint`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, local `pnpm smoke:browser`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; guide now describes Cursor, Codex, Claude Code, and Google Antigravity as named connection choices |
| 2026-05-25 | Updated the user test guide for current STEP 1 and list-management flows | `6c35d12` | Docs-only; pushed to `main`, production smoke passed | `pnpm release:check`, `pnpm smoke:prod`; guide now matches context-sensitive STEP 1 actions and checkbox bulk controls on active/deleted idea lists |
| 2026-05-25 | Added bulk restore and permanent delete controls to the deleted ideas list | `1720d6c` | Pushed to `main`; production smoke passed | `pnpm lint`, `pnpm typecheck`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; deleted ideas now support checkbox selection, selected restore, and selected/all permanent delete with related records cleaned first |
| 2026-05-25 | Simplified STEP 1 first-run AI actions by showing only context-relevant buttons | `21394bc` | Pushed to `main`; production smoke passed | `pnpm lint`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; empty input now shows one AI idea-discovery action, pasted input shows one AI organize action, and candidate refresh appears only after generated candidates exist |
| 2026-05-25 | Kept result type separate from build delivery across downstream packages | `4957f03` | Pushed to `main`; production smoke passed | `pnpm lint`, `pnpm typecheck`, `pnpm release:check`, `pnpm quality:full`, `pnpm smoke:build-sync`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; manual intake now stores build-delivery preference and final packages use the selected external tool rather than a default Cursor path |
| 2026-05-25 | Clarified market-scan estimate fallback and quota diagnostics | `8f9b9df` | Pushed to `main`; production smoke passed | `node --check scripts/smoke_market_scan.mjs`, `pnpm lint`, `pnpm typecheck`, `pnpm release:check`, `MARKET_SCAN_SMOKE_ALLOW_ESTIMATE=1; pnpm smoke:market`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`; strict `pnpm smoke:market` is blocked by OpenAI quota/billing until the production API project has quota again |
| 2026-05-25 | Simplified STEP 8 and aligned first-use step labels | `1c89769` | Pushed to `main`; production smoke passed | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-25 | Promoted Claude Code and Google Antigravity to named live connector paths | `bd1c429` | Pushed to `main`; production smoke passed after SQL `20260525020000_allow_named_build_sync_tools.sql` | `pnpm quality:full`, `pnpm smoke:build-sync`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-25 | Promoted Codex from package-only handoff to live external connector path | `a527c63` | Pushed to `main`; production smoke passed after SQL `20260525010000_allow_codex_build_sync_tokens.sql` | `pnpm quality:full`, `pnpm smoke:build-sync`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-25 | Limited first supported external build choices to Cursor, Codex, Claude Code, and Google Antigravity | `b6b939b` | Pushed to `main`; production alias updated | `pnpm quality:full`, `pnpm release:check`, `pnpm smoke:prod`, `pnpm smoke:routes`, `node .\scripts\smoke_browser.mjs`, `pnpm smoke:build-sync` |
| 2026-05-24 | Refreshed the user test guide for current STEP 1 and Cursor sync wording | Current commit | Docs-only; guide now matches current first-run buttons and final execution connector checks | `pnpm release:check` |
| 2026-05-24 | Updated public guide for Cursor automatic progress sync | Current commit | User-facing guide copy; production browser smoke now checks Cursor CLI and backup wording | `node --check scripts/smoke_browser.mjs`, `pnpm quality:full`, `pnpm smoke:browser` |
| 2026-05-24 | Documented the safe write-back boundary for non-Cursor connectors | Current commit | Docs-only connector governance; no runtime deploy required | `pnpm release:check` |
| 2026-05-24 | Locked non-Cursor external tools to package-only final execution guidance | Current commit | Script/docs/copy hardening; production smoke checks non-Cursor tools do not expose Cursor setup | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm smoke:build-sync` |
| 2026-05-24 | Extended Cursor sync smoke through STEP 7 final execution CLI guidance | Current commit | Script/docs only; disposable launch package verifies Cursor final execution guidance | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm smoke:build-sync` |
| 2026-05-24 | Packaged Cursor handoff as a local CLI/MCP connector | Current commit | User-facing final execution package change; production deploy required after commit | `pnpm lint`, `pnpm typecheck` |
| 2026-05-24 | Extended Cursor sync smoke through STEP 8 task-board rendering | Current commit | Script/docs only; production UI and disposable sync path verified | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm smoke:build-sync` |
| 2026-05-24 | Added production smoke for revocable Cursor sync tokens | Current commit | Script/docs only; production registry verified after SQL | `node --check scripts/smoke_build_sync_tokens.mjs`, `pnpm smoke:build-sync` |
| 2026-05-24 | Added stored hashed Cursor sync tokens and individual revoke controls | Current commit | Deploy-safe with legacy fallback; registry enforcement activates after production SQL | `pnpm quality:full` |
| 2026-05-24 | Made STEP 7/8 task sync automatic-first and folded manual progress import into a backup path | `4f48767` | Pushed to `main`; production alias updated | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth` |
| 2026-05-24 | Documented Cursor build sync token lifecycle and current revocation boundary | Current commit | Docs-only; no runtime deploy required | `pnpm release:check` |
| 2026-05-24 | Simplified STEP 8 into a task-led learning review before telemetry details | `0ed9adc` | Pushed to `main`; production alias `https://ai-venture-lab.vercel.app` updated | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, direct `node ./scripts/smoke_browser_auth.mjs` after one transient `pnpm smoke:browser:auth` spawn EPERM |
| 2026-05-24 | Clarified Cursor live sync versus Codex, Claude Code, Antigravity, and generic MCP package handoffs | `886c8a6` | Pushed to `main`; production alias updated | `pnpm typecheck`, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth` |
| 2026-05-24 | Refreshed synced Cursor tasks from the server before showing final execution status | `c54a208` | Pushed to `main`; production alias updated | `pnpm quality:full`, production smoke and browser/auth smoke passed |
| 2026-05-24 | Documented the Cursor connector contract and scoped write-back boundary | `ff6495f` | Pushed to `main`; production alias updated | Connector docs/readback and standard release checks passed |
| 2026-05-24 | Updated user-facing Cursor setup guidance for project-root install and Composer start prompt | `a6ff25e` | Pushed to `main`; production alias updated | Guide readback and standard release checks passed |
| 2026-05-24 | Enabled scoped Cursor MCP write-back for implementation task progress | `44618ed` | Pushed to `main`; production alias updated | `pnpm quality:full`, production smoke and browser/auth smoke passed |
| 2026-05-22 | Replaced stray English micro-labels in workbench screens | `b8a6e6d` | Pushed to `main`; production smoke passed after Git integration | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes` after one transient local `spawn EPERM`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `pnpm smoke:market` (`openai_web`, 5 sources, 5 competitors); GitHub Actions run `26287652988` passed on Node 20 and Node 24 |
| 2026-05-22 | Added first-build bridge cards to the STEP 5 package summary | `c3439e7` | Pushed to `main`; production smoke passed after Git integration | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `pnpm smoke:market` (`openai_web`, 5 sources, 5 competitors); GitHub Actions run `26287357653` passed on Node 20 and Node 24 |
| 2026-05-22 | Hardened market scan smoke timeout for web-search latency | `c517187` | Pushed to `main`; production smoke passed after Git integration | `node --check scripts/smoke_market_scan.mjs`, `pnpm release:check`, `pnpm smoke:prod`, `pnpm smoke:market` (`openai_web`, 5 sources, 5 competitors); GitHub Actions run `26287064155` passed on Node 20 and Node 24 |
| 2026-05-22 | Reduced first-run idea input actions to one visible execution path | `8bde93c` | Pushed to `main`; production smoke passed after Git integration | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `MARKET_SCAN_SMOKE_TIMEOUT_MS=180000; pnpm smoke:market` (`openai_web`, 5 sources, 5 competitors); GitHub Actions run `26286534672` passed after rerunning a transient Node 20 setup failure |
| 2026-05-22 | Polished production-package terminology and smoke env boundaries | `23c5005` | Pushed to `main`; production smoke passed after Git integration, direct Vercel inspect remains blocked by local certificate chain | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:market` (`openai_web`, 5 sources, 5 competitors), `pnpm smoke:browser`, `pnpm smoke:browser:auth`; GitHub Actions run `26285879538` passed on Node 20 and Node 24 |
| 2026-05-22 | Added market and competition auto-research smoke command | `3d11974` | Pushed to `main`; script/docs only, production shell smoke passed | `node --check scripts/smoke_market_scan.mjs`, `pnpm smoke:market` (`openai_web`, 5 sources, 5 competitors), `pnpm quality:full`, `pnpm smoke:prod`; GitHub Actions run `26285315809` passed on Node 20 and Node 24 |
| 2026-05-22 | Made development package design and architecture context result-type-specific | `e70270a` | Pushed to `main`; production smoke passed after Git integration | `pnpm quality:full`, `pnpm release:check`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`; GitHub Actions run `26284899772` passed on Node 20 and Node 24 |
| 2026-05-22 | Reframed the user test guide around the automation-first happy path | Current commit | Docs-only; no runtime deploy intended beyond Git integration | `pnpm release:check`; stale manual-first button names remain only as advanced operator checks |
| 2026-05-22 | Simplified STEP 1 first-click path to one primary run action | `d414133` | Pushed to `main`; production smoke passed after Git integration; direct Vercel CLI inspect remains blocked by local certificate chain | `pnpm quality:full`, `pnpm release:check`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, production Playwright STEP 1 single-action check |
| 2026-05-22 | Confirmed latest GitHub Actions quality gate pass | Current commit | GitHub Actions only; no runtime deploy intended | Quality Gate run `26282567364` passed on `main` for Node 20 and Node 24; workflow remains read-only, no secrets, no deploys, no authenticated/telemetry smoke |
| 2026-05-22 | Reran RLS allowed/denied browser smoke with retained disposable fixtures | Current commit | Smoke-only; no app deploy or production mutation | `pnpm smoke:browser:rls:preflight`, `pnpm smoke:browser:rls`; anonymous denied, A/B allowed, A->B and B->A denied, no writes, no screenshots, no secret output |
| 2026-05-22 | Reran telemetry ingest and funnel smoke after local secret alignment | Current commit | Smoke-only; no app deploy or production mutation | `pnpm smoke:telemetry`, `pnpm smoke:telemetry:funnel`; disposable idea data, summary-only evidence, no secret values recorded |
| 2026-05-22 | Hardened telemetry smoke env loading and checked rerun readiness | `efd0059` | Blocker cleared after local secret alignment; no app deploy or production mutation | Telemetry smoke now loads local telemetry env names and sends full single-event names; initial rerun reached production but returned HTTP 401 because the local telemetry secret was rejected; no secret values read back or recorded |
| 2026-05-22 | Updated authenticated write smoke for the optional collaboration setup flow | Current commit | Script/docs only; no product deploy required beyond normal Git integration | `node --check scripts/smoke_browser_auth.mjs`, `pnpm smoke:browser:auth`, `BROWSER_SMOKE_ALLOW_WRITE=1; pnpm smoke:browser:auth`; disposable smoke-prefixed idea/package, no secret output |
| 2026-05-22 | Polished full-preview production package language for manager-facing terms | `03189ae` | Pushed to `main`; production smoke passed after Git integration | `pnpm quality:full`, `pnpm release:check`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth` |
| 2026-05-22 | Aligned result type with implementation tasks and current market scans | `db0c91e`, `8d8b1da` | Pushed to `main`; Vercel Git integration expected to deploy production, direct Vercel CLI deploy remains blocked by local certificate chain | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `pnpm smoke:browser:rls` |
| 2026-05-22 | Stabilized browser smoke env loading and production smoke marker | `c3e0b1a`, `363f25b` | Script-only; no product deploy required beyond normal Git integration | `pnpm quality:full`, authenticated browser smoke, RLS allowed/denied browser smoke, production smoke |
| 2026-05-22 | Documented the external package contract and clarified first-use/user-facing labels | `11375bd`, `6bbc473`, `fec146d` | Pushed to `main`; production smoke passed after Git integration | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth`, `pnpm smoke:browser:rls` |
| 2026-05-18 | Implemented WQ-061 P1 first-use clarity fixes | Current commit | Production deploy required after commit | `first_run_clarity_implemented`, `candidate_save_bridge_implemented`, `validation_experiment_guidance_implemented`; pending lint/typecheck/build/prod smoke |
| 2026-05-17 | Configured app Node 20/24 compatibility matrix | Current commit | GitHub Actions only; no runtime deploy intended | `ci_app_node_matrix_20_24_configured`, `no_runtime_selection_change`, no secrets/deploys/production mutation; pass evidence requires pushed GitHub matrix run |
| 2026-05-17 | Recorded app Node 20/24 matrix pass evidence | Current commit | GitHub Actions only; no runtime deploy intended | `ci_app_node_matrix_passed`, run `25985698631`, Node 20/24 jobs passed; no secrets/deploys/production mutation |
| 2026-05-17 | Added controlled beta session playbook | Current commit | Docs/check-script only; no runtime deploy intended | `controlled_beta_session_playbook`, `synthetic_beta_data_only`, `summary_only_beta_feedback`, no secrets/production mutation |
| 2026-05-17 | Added controlled beta feedback template | Current commit | Docs/template/check-script only; no runtime deploy intended | `controlled_beta_session_feedback_template`, `summary_only_beta_feedback_template`, `smoke_evidence_not_user_feedback`, no secrets/production mutation |
| 2026-05-17 | Added controlled beta participant screener | Current commit | Docs/template/check-script only; no runtime deploy intended | `controlled_beta_participant_screener`, `no_contact_details_in_repo`, `scheduling_outside_repo_artifacts`, no secrets/production mutation |
| 2026-05-17 | Prepared first controlled beta session | Current commit | Docs/check-script only; no runtime deploy intended | `controlled_beta_session_001_prep`, `copy_ready_synthetic_input`, `technical_stack_friction_probe`, no secrets/production mutation |
| 2026-05-17 | Added controlled beta feedback triage | Current commit | Docs/template/check-script only; no runtime deploy intended | `controlled_beta_feedback_triage`, `feedback_action_rules`, `feedback_triage_decision_output`, no secrets/production mutation |
| 2026-05-17 | Tested upcoming CI runtime/image path | Current commit | GitHub Actions only; no runtime deploy intended | `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24`, `windows-2025-vs2026`, no CI scope expansion |
| 2026-05-17 | Recorded CI runner/action notices | Current commit | Docs/check-script only; no runtime deploy intended | `Quality Gate` run `25984007955` passed; Node.js 20 action runtime and `windows-latest` redirect notices recorded as non-blocking maintenance risk |
| 2026-05-17 | Enabled read-only GitHub Actions quality gate | Current commit | Git push may still trigger Vercel auto-deploy through the connected production project; workflow itself does not deploy | `.github/workflows/quality.yml`, `pnpm release:check`, `pnpm quality:full`, CI boundary readback, forbidden workflow pattern guard |
| 2026-05-17 | Approved controlled beta ship evidence | Current commit | Production LKG `dpl_72EhMSpuaz8r4PcjvPZ7uHipa8Sa`; docs/check-script only in repo | Vercel inspect, `pnpm quality:full`, launch evidence packet, rollback evidence |
| 2026-05-17 | Added launch evidence packet and ship guard | Current commit | Skipped, docs/check-script only; no runtime deploy | launch/security subagent review, `pnpm harness:check`, `pnpm release:check`, ship-state consistency check, docs/templates obvious-secret pattern scan |
| 2026-05-17 | Added public beta launch evidence packet | Current commit | Skipped, docs/check-script only; no runtime deploy | launch evidence packet readback, `pnpm harness:check`, `pnpm release:check` |
| 2026-05-17 | Added controlled beta launch gate summary | Current commit | Skipped, docs/check-script only; no runtime deploy | launch gate review, `pnpm harness:check`, `pnpm release:check` |
| 2026-05-17 | Added disposable smoke cleanup runbook | Current commit | Skipped, docs/check-script only; no runtime deploy | subagent cleanup review, `pnpm harness:check`, `pnpm release:check`, `pnpm quality:full` |
| 2026-05-17 | Aligned beta evidence docs with passed auth/RLS/telemetry gates | Current commit | Skipped, docs/check-script only; no runtime deploy | stale keyword grep, `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-17 | Closed telemetry secret rotation blocker | `8ba6b0b` | Vercel Production redeployed after env rotation | `pnpm smoke:telemetry`, `pnpm smoke:telemetry:funnel`, no secret value recorded |
| 2026-05-17 | Recorded RLS allowed/denied browser evidence | `4bba176` | Smoke-only/no app redeploy | `pnpm smoke:browser:rls`, anonymous denied, A/B allowed, A->B/B->A denied |
| 2026-05-17 | Stabilized authenticated write smoke | `f05e1d8` | Production alias after deploy | `pnpm lint`, `pnpm typecheck`, `pnpm build`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, `pnpm smoke:browser:auth` |
| 2026-05-16 | WQ-047 refreshed public beta manager checklist | Current commit | Skipped, docs-only/no runtime change | checklist readback, `pnpm release:check`, subagent beta closeout review |
| 2026-05-16 | WQ-046 ran manager workspace regression and copy cleanup | Current commit | Production auto-deploy after push | `pnpm lint`, `pnpm typecheck`, `pnpm build`, `pnpm release:check`, local `pnpm smoke:routes`, local `pnpm smoke:browser`, subagent QA/copy review, Playwright desktop/mobile visual check, production `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser`, production Playwright copy check |
| 2026-05-16 | WQ-045 refined detailed workbench manager language | Current commit | Production auto-deploy after push | `pnpm lint`, `pnpm typecheck`, `pnpm build`, local `pnpm smoke:routes`, local `pnpm smoke:browser`, subagent copy review, production `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-16 | WQ-044 added manager-facing executive decision panel | Current commit | Production auto-deploy after push | `pnpm lint`, `pnpm typecheck`, `pnpm build`, local `pnpm smoke:routes`, local `pnpm smoke:browser`, Playwright desktop/mobile visual check, production `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-16 | WQ-042 added disposable RLS fixture handoff | Current commit | Skipped, docs-only/no execution | Fixture handoff keyword checks, `pnpm release:check` |
| 2026-05-16 | WQ-041 reviewed Supabase RLS policy posture | Current commit | Skipped, static SQL/docs-only | SQL policy readback, posture keyword checks, `pnpm release:check` |
| 2026-05-16 | WQ-040 added blocked-safe RLS smoke runner scaffold | Current commit | Skipped, script/docs-only | `node --check`, `pnpm smoke:browser:rls:preflight`, `pnpm release:check`, `pnpm lint`, `pnpm typecheck` |
| 2026-05-16 | WQ-039 documented RLS allowed/denied smoke plan | Current commit | Skipped, docs-only/no deploy | Docs readback for plan, evidence fields, stop conditions, no mutation |
| 2026-05-16 | WQ-038 ran authenticated browser visibility smoke | Current commit | Skipped, smoke-only/no deploy | Safe env preflight, `pnpm smoke:browser:auth`; write/create/screenshot flags disabled |
| 2026-05-16 | WQ-037 documented CI workflow scope boundary | Current commit | Skipped, docs/script-only | Keyword readback, `pnpm quality:full` |
| 2026-05-16 | WQ-036 documented beta env and smoke boundary | Current commit | Skipped, docs/env-example only | Keyword readback, `pnpm release:check` |
| 2026-05-12 | Repaired workspace creation ownership and RLS handoff | Current commit | Production alias after deploy | `pnpm lint`, `pnpm typecheck`, harness check, release check, `pnpm build` |
| 2026-05-12 | Routed signed-in operators to team-space check before idea intake | Current commit | Production alias after deploy | `pnpm lint`, direct `tsc`, harness check, release check, `pnpm build` |
| 2026-05-12 | Switched operator access to password-first login | Current commit | Production alias after deploy | `pnpm lint`, direct `tsc`, harness check, release check, `pnpm build` |
| 2026-05-12 | Clarified email-link login copy for non-technical operators | Current commit | Production alias after deploy | `pnpm lint`, direct `tsc`, harness check, release check, `pnpm build` |
| 2026-05-12 | Made login the default first step for non-technical operators | Current commit | Production alias after deploy | `pnpm lint`, direct `tsc`, harness check, release check, `pnpm build` |
| 2026-05-12 | Hid developer-only telemetry details behind a manager-facing handoff panel | Current commit | Production alias after deploy | `pnpm lint`, direct `tsc`, harness check, release check, `pnpm build` |
| 2026-05-12 | Refined workbench panels for manager-facing decision language | Current commit | Production alias after deploy | `pnpm lint`, direct `tsc`, harness check, release check, `pnpm build` |
| 2026-05-12 | Refined manager-oriented login, navigation, and guide copy | Current commit | Production `FDPRsS6LESA9LWaZe7XG3BdkWa52` | `pnpm lint`, direct `tsc`, harness check, release check, `pnpm build`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-12 | Added full product funnel telemetry smoke | Current commit | Production alias after deploy | Missing-secret guard, `pnpm lint`, direct `tsc`, harness check, release check, `pnpm build` |
| 2026-05-06 | Added external MVP telemetry integration kit | Current commit | Production alias after deploy | `pnpm lint`, direct `tsc`, harness check, release check, `pnpm build` |
| 2026-05-06 | Re-deployed product telemetry ingest with Production secrets | `5414835` | Production `2AM2imxYxi3oh7gMs6KDG756pn1d` | `pnpm smoke:prod`, `pnpm smoke:routes`, `node .\scripts\smoke_browser.mjs` |
| 2026-05-06 | Added authenticated telemetry ingest smoke harness | Current commit | Skipped, script/docs-only | `pnpm quality:full`, direct `smoke_routes.ps1`, direct `smoke_production.ps1`, missing-secret guard |
| 2026-05-06 | Added product telemetry funnel and taxonomy | `5414835` | Production `2AM2imxYxi3oh7gMs6KDG756pn1d` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `node .\scripts\smoke_browser.mjs` |
| 2026-05-06 | Added product telemetry ingest adapter | `1efc76e` | Production `2AM2imxYxi3oh7gMs6KDG756pn1d` | `pnpm quality:full`, route smoke |
| 2026-05-06 | Added learning telemetry dashboard and event capture | `809929d` | Production `2mG3B8h47DPs9oURaRDnEVRN5Mv4` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Closed core public beta readiness phase | Current commit | Skipped, docs-only | `pnpm quality:full` |
| 2026-05-06 | Added post-launch learning loop | `f3f39d9` | Production `3hEuawDqF1GatK6UkD7jS4mTSN76` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added QA acceptance matrix | `75be6c9` | Production `2igHVoFtCvHJV6YTCWQNmMNQcm9k` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added MVP build command packet | `f55ab6e` | Production `DGYx1NBtDdxoxcbbWcN2pyWBptqA` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added release decision packet | `2864983` | Production `4Xn1xGLyYHKSJP3L4dwNLbjjBF5C` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added artifact approval review summaries | `78728c1` | Production `AZozcZkst16gxofeYYL7AQGMUhs4` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added implementation dependency gate | `3058361` | Production `BStiaT3PxRdAzjGz1sEaZjM9g5wG` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added backend execution checklist | `9987128` | Production `HNHi9Jwhydvg23fLTpmYtCC9SYrM` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added artifact review queue | `8d9caf8` | Production `8zavhTmBB2MXRBRyQn5vikmJMMtY` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added extraction replay comparison | `f67171b` | Production `6arqYg8F2xiWVzW76fATpwdbXdQv` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added extraction run metadata | `9f68904` | Production `BUaguUzx47Rr2qppBveCjPHY4GFL` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added MVP scaffold manifest | `caef916` | Production `k6M2buEpQfam66eoy4MJBUiQSuoV` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added extraction business/development scorecard | `b2d694d` | Production `3ghP4ELrQJjz9mh42nEuof4i41XB` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added bulk extraction package save | `b789935` | Production `F1XxGBXBL42ZwYuk7Tt1CtzMKY3K` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added app blueprint implementation handoff | `76bb857` | Production `89BE2gPhGMuJikTE1b2NrvfDbXJ3` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-18 | Refined first-use Korean copy and user-facing labels | `eeae73e` | Production `4reHEsieWeUVYTkF5sPuktuJuWGm` | `pnpm lint`, `pnpm typecheck`, `pnpm build`, `pnpm release:check`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-18 | Added first build bridge to execution board | `78bfb82` | Production `55npDZVjy4Fi9RNPofKnpmHZ2N49` | `pnpm lint`, `pnpm typecheck`, `pnpm build`, `pnpm release:check`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added design generation prompt for app builders | `814d8cc` | Production `3tkMUk24xjekGq41ah7gc85LKMmP` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added one-click development artifact package save | `3b1c5e8` | Production `2Sc3JTq2Co6U5VhAcvXvjq7raB3X` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes`, `pnpm smoke:browser` |
| 2026-05-06 | Added optional authenticated browser smoke scaffold | `5a0ffed` | Skipped, script/docs-only | `pnpm quality:full`, `pnpm smoke:browser` |
| 2026-05-06 | Added browser smoke screenshot artifact handling | `98108bd` | Skipped, script/docs-only | `pnpm smoke:browser` |
| 2026-05-06 | Added browser-level beta smoke harness | `dc71fe2` | Skipped, script/docs-only | `pnpm quality:full`, `pnpm smoke:browser` |
| 2026-05-06 | Added implementation agent run packages | `48de7c3` | Production `dpl_xsbb9d7SMFtSyUheymiwdof55Bkm` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes` |
| 2026-05-06 | Added development kickoff guardrails | `602f5c4` | Production `dpl_F9yQJGcVFNfWQcBqw81nKmXSyMB7` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes` |
| 2026-05-06 | Added MVP slice planner | `dea14ed` | Production `dpl_FDaMV9rPosmHp4BeRuGN2LYzK7pJ` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes` |
| 2026-05-06 | Added PRD readiness handoff artifacts | `2ff32f2` | Production `dpl_3DGFMeUpdmHpV5fxy7UPW1EKj8iP` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes` |
| 2026-05-06 | Added validation evidence coach | `e5c08f0` | Production `dpl_EFhJyFX5xXo6B6F53MUYjYvaAud7` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes` |
| 2026-05-06 | Added saved extraction portfolio reports | `b22baac` | Production `dpl_BagnfW2URR5KisnQGMrzZDkSazrV` | `pnpm quality:full`, `pnpm smoke:prod`, `pnpm smoke:routes` |
| 2026-05-06 | Added extraction candidate comparison matrix | Current commit | Pending production deploy | `pnpm lint`, `pnpm typecheck` |
| 2026-05-06 | Added optional OpenAI-backed idea extraction with local fallback | Current commit | Pending production deploy | `pnpm quality:full`, local `pnpm smoke:routes` |
| 2026-05-05 | Added artifact source filtering | Current commit | Production | `pnpm quality:full`, `pnpm smoke:prod` |
| 2026-05-05 | Added development handoff history hints | Current commit | Production | `pnpm quality:full`, `pnpm smoke:prod` |
| 2026-05-05 | Added saved filtered implementation handoff artifacts | Current commit | Production | `pnpm quality:full`, `pnpm smoke:prod` |
| 2026-05-05 | Added filtered implementation run prompts | Current commit | Production | `pnpm quality:full`, `pnpm smoke:prod` |
| 2026-05-05 | Added filtered development backlog export | Current commit | Production | `pnpm quality:full`, `pnpm smoke:prod` |
| 2026-05-05 | Added implementation task board filters | Current commit | Production | `pnpm quality:full`, `pnpm smoke:prod` |
| 2026-05-05 | Added blocked-task owner and next-action hints | Current commit | Production | `pnpm quality:full`, `pnpm smoke:prod` |
| 2026-05-05 | Added evidence-priority summary for implementation tasks | Current commit | Production | `pnpm quality:full`, `pnpm smoke:prod` |
| 2026-05-05 | Added release evidence summary to completion reports | Current commit | Production | `pnpm quality:full`, `pnpm smoke:prod` |
| 2026-05-05 | Added implementation-task evidence quality hints | Current commit | Production | `pnpm quality:full`, `pnpm smoke:prod` |
| 2026-05-05 | Added operator release evidence examples | Current commit | Skipped, docs-only | `pnpm quality:full` |
| 2026-05-05 | Added local release-readiness command | Current commit | Skipped, script/docs-only | `pnpm quality:full`, `pnpm release:check` |
| 2026-05-05 | Added app development release safeguards | Current commit | Production | `pnpm quality:full`, `pnpm smoke:prod` |
| 2026-05-05 | Added extraction proceed/research/pivot/kill gates | Current commit | Production | `pnpm quality:full`, `pnpm smoke:prod` |
| 2026-05-05 | Added route smoke checks | Current commit | Skipped, script/docs-only | `pnpm quality:full`, `pnpm smoke:routes` |
| 2026-05-05 | Added phase status ledger | `4745c1b` | Skipped, docs/script-only | `pnpm quality:full` |
| 2026-05-05 | Formalized the agent work loop in repo guidance | `c420fde` | Skipped, docs-only | `pnpm quality:full` |
| 2026-05-05 | Expanded harness file checks | `a3a0c41` | Skipped, script/docs-only | `pnpm quality:full` |
| 2026-05-05 | Added shell priority candidates | `a77de32` | Production | `pnpm quality:full`, `pnpm smoke:prod` |
| 2026-05-05 | Redacted extracted source excerpts before persistence | `7a6a297` | Production | `pnpm quality:full`, `pnpm smoke:prod` |
| 2026-05-05 | Added role prompt pack handoff | `029eeac` | Production | `pnpm quality:full`, `pnpm smoke:prod` |

## Skipped Or Deferred

| Item | Type | Reason | Next Handling |
| --- | --- | --- | --- |
| GitHub Actions runner/action notices | Monitoring | Latest approved check run passed, but GitHub still reports the Node.js 20 JavaScript action deprecation notice while forcing listed actions to Node 24 | Current workflow opts into Node24 JavaScript action runtime and `windows-2025-vs2026`, and app build compatibility passed on Node 20/24 without secrets or deploys |
| GitHub Actions workflow push | Completed | User approved workflow creation; `.github/workflows/quality.yml` mirrors `pnpm quality:full` with `permissions: contents: read` | Keep CI no-secret/no-deploy; local `pnpm quality:full` and production smoke remain required release evidence |
| Browser-level authenticated write smoke execution | Completed | Explicit per-run approval was granted and disposable workspace/idea data was used; the 2026-05-22 rerun passed after aligning the smoke runner with the optional collaboration setup flow | Rerun only with explicit approval, disposable data, and cleanup ownership |
| RLS allowed/denied smoke execution | Completed | Two disposable accounts and two private workspace labels passed anonymous/allowed/denied checks; the 2026-05-22 approved rerun passed with no writes, screenshots, telemetry, or secret output | Rerun when fixtures, RLS policy, migration, or workspace access code changes |
| Production RLS migration confirmation | Completed | Production posture was checked before denied smoke: required private-read migrations were present, old public-read policies were absent, and RLS was enabled on core tables | Rerun posture checks when migrations or policies change |
| Telemetry production smoke execution | Completed | Post-rotation telemetry ingest and funnel smoke passed with a disposable idea id | Rerun when telemetry env, endpoint behavior, or telemetry RLS policies change |
| Authenticated telemetry smoke execution | Completed | 2026-05-22 approved rerun passed after local telemetry secret alignment, using disposable idea data and summary-only evidence | Rerun only when telemetry env, endpoint behavior, or telemetry RLS policies change |
| Codex, Claude Code, Antigravity live write-back | Completed | Cursor, Codex, Claude Code, and Google Antigravity now receive tool-specific setup files, scoped build-sync tokens, local progress commands, revoke controls, and denied-case smoke coverage | Keep `pnpm smoke:build-sync` as the regression check before changing token issuance, revoke, setup files, or progress sync |
| Generic MCP user-facing selector | Deferred | The operator chose named tool integrations first; generic MCP is too vague for the current product promise and could imply unsupported automation | Keep legacy compatibility only; revisit after named connectors and a concrete resource contract exist |
| Cursor token rotation and revocation controls | Completed | Production SQL was applied and `pnpm smoke:build-sync` verified registry ready, connection revoke, and revoked-token rejection | Keep `pnpm smoke:build-sync` as the regression check before changing token issuance, revoke, or progress sync |
| Native internal builder | Deferred | Final execution can choose an internal build destination, but the actual native builder is not implemented yet | Keep the handoff boundary explicit until a first internal build runtime is scoped |
| Strict web-sourced market scan smoke | Completed | After the OpenAI key rotation, strict smoke reached the web path once but exposed a user-input placeholder source, then fell back because the OpenAI response was not parsed as the market-scan schema; the hardened route now forces hosted web search, filters public sources, and records clearer fallback diagnostics | Keep `pnpm smoke:market` as the regression check when market-scan prompts, model settings, source normalization, or OpenAI/Vercel env settings change |
| Build-sync disposable spend smoke | Completed | Production SQL was applied; trusted top-ups exposed and closed the STEP 6 direct URL allowlist, `T-001` visibility, and STEP 8 state-aware smoke assertion gaps; the 2026-05-29 full rerun passed with registry ready, four named connector setup files, write-back, UI, revoke, and denied-case coverage | Keep `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync` as the opt-in regression check when token issuance, credit enforcement, setup downloads, progress sync, STEP 7, or STEP 8 changes; expect it to consume one 30-credit disposable build pass |

## Next User Actions

Use `docs/BETA_ENV_AND_SMOKE_BOUNDARY.md` before beta smoke, telemetry smoke, env changes, or deployment evidence collection. The boundary is names-only and forbids `.env.local` readback, secret output, production mutation, deploy trigger, rollback, paid API calls, credential/session handling, and `D:\Projects\AdMate` mutation.

Use `docs/CI_WORKFLOW_SCOPE_BOUNDARY.md` before modifying GitHub Actions. The current posture is `ci_workflow_scope_active`: `.github/workflows/quality.yml` mirrors `pnpm quality:full` with `permissions: contents: read`, no secrets, no deploys, no authenticated write smoke, no telemetry smoke, and no production mutation.

Use `docs/APP_NODE_RUNTIME_POSTURE.md` before adding `engines.node`, changing the Vercel Project Settings Node.js version, or treating CI matrix success as a production runtime migration. Current posture is `ci_app_node_matrix_passed` with `no_runtime_selection_change`.

Authenticated visibility and explicit write smoke have passed against `https://ai-venture-lab.vercel.app` with disposable Supabase Auth credentials loaded locally and not printed. The write run used disposable data and summary-only evidence.

Use `docs/RLS_ALLOWED_DENIED_SMOKE_PLAN.md`, `docs/SUPABASE_RLS_POLICY_POSTURE_REVIEW.md`, `docs/RLS_DISPOSABLE_FIXTURE_HANDOFF.md`, and `pnpm smoke:browser:rls:preflight` before rerunning any cross-workspace, second-account, or denied-case smoke. Current RLS allowed/denied evidence has passed with disposable account/workspace fixtures and summary-only evidence.

Optional: add `OPENAI_API_KEY` and, if desired, `OPENAI_IDEA_MODEL` to Vercel Production to enable server-side AI extraction. Without it, the app automatically falls back to the local rules engine.

Market/competition auto-research is configured. After the 2026-05-27 OpenAI key rotation and hardened route deploy, strict `pnpm smoke:market` passed with `mode=openai_web`, `model=gpt-5-mini`, 5 public sources, and 5 competitors. The fallback estimate path remains working and labeled as `추정 초안`.

Build-sync credit spend smoke passed again on 2026-05-29 after the trusted smoke account was topped up to 120 credits. The latest STEP 8 sync-review rerun sequence consumed two disposable 30-credit build passes: the first exposed a smoke selector issue after cleanup, and the second passed with the four named connector paths and denied cases verified. The follow-up billing smoke confirmed 30 credits remain. Future reruns of `BUILD_SYNC_SMOKE_ALLOW_BUILD_PASS_SPEND=1 pnpm smoke:build-sync` should be treated as intentional spend checks and consume one 30-credit disposable build pass unless a pre-unlocked disposable smoke idea is used.

Learning telemetry writes passed again on 2026-05-22 after local telemetry secret alignment, using a disposable idea id and summary-only evidence. Reconfirm `telemetry_events` table posture and RLS policies only when telemetry migrations, endpoint behavior, or production environment settings change.

Completed for external MVP event ingest: `SUPABASE_SERVICE_ROLE_KEY` and rotated `TELEMETRY_INGEST_SECRET` are present in Vercel Production. Keep `TELEMETRY_INGEST_SECRET` only in trusted server environments, never in browser bundles. Post-rotation `pnpm smoke:telemetry` and `pnpm smoke:telemetry:funnel` passed with disposable idea data and no secret value recorded.

Use `docs/SMOKE_DATA_CLEANUP_RUNBOOK.md` to record whether disposable write-smoke data, reusable RLS fixture pairs, telemetry smoke events, and any local screenshots are `completed_cleanup`, `retained_for_rerun`, or `not_applicable`. Current cleanup posture: `smoke_cleanup_user_action_recorded`, `cleanup_status_not_applicable_or_owner_confirmed`, `cleanup_disposition_closed`.

Use `docs/PUBLIC_BETA_LAUNCH_GATE.md` before changing the controlled beta decision. Current launch gate keywords: `public_beta_launch_gate`, `launch_gate_decision_ship`, `cleanup_disposition_closed`.

Use `docs/PUBLIC_BETA_LAUNCH_EVIDENCE_PACKET.md` as the controlled beta decision packet. Current evidence-packet keywords: `public_beta_launch_evidence_packet`, `final_operator_decision_ship`, `last_known_good_deployment_recorded`.

For named external handoff tests, use `docs/EXTERNAL_PRODUCTION_PACKAGE.md` for the common contract and `docs/CURSOR_EXTERNAL_TOOL_GUIDE.md` for the Cursor-specific project-root install. The default path is: choose Cursor, Codex, Claude Code, or Google Antigravity in STEP 7, download that tool's setup or connection file, move it to the real external project root, run the generated command, open the selected tool, then start from the generated `AI_VENTURE_*_START.md` file. Manual JSON paste is only the backup path.

## Next Jobs

1. Keep named external build tools focused on Cursor, Codex, Claude Code, and Google Antigravity; run `pnpm smoke:build-sync` before changing token issuance, revoke, setup files, or progress sync.
2. Keep generic MCP deferred until a concrete user-facing connector, permission model, denied-case smoke, and rollback path exist.
3. Continue the active product hardening queue with first-use simplicity and task-led execution monitoring before broad new feature scope.
4. Keep RLS, telemetry, authenticated write smoke, and GitHub Actions changes behind their existing explicit-approval and disposable-data boundaries.
